/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 * lcm-gen 1.5.2
 */

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
package aspn23_lcm;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
/**
 * 3D direction to or from a point as defined by the reference enum. Information about the remote
 * point (its position and/or correspondence with previous or future appearances
 * of this same point) may be determined using remote_point.
 */
public final class type_direction_3d_to_point implements lcm.lcm.LCMEncodable
{
    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    public byte icd_type_direction_3d_to_point;

    /**
     * Description: Position and/or correspondence information about the remote point.
     * Units: various
     */
    public aspn23_lcm.type_remote_point remote_point;

    /**
     * Defines measurement reference frame.
     */
    public byte reference_frame;

    /**
     * Description: 3D direction to the remote point as defined in reference_frame.
     * Units: various
     * LCM Type: double[2]
     */
    public double obs[];

    /**
     * Description: Covariance of the direction measurement as defined in reference_frame.
     * Units: various
     * LCM Type: double[2][2]
     */
    public double covariance[][];

    /**
     * Description: Switch for whether observation_characteristics is valid or not.
     * Units: none
     */
    public boolean has_observation_characteristics;

    /**
     * Description: Image feature characteristics.
     * Units: various
     */
    public aspn23_lcm.type_image_feature observation_characteristics;

    /**
     * Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).
     */
    public byte error_model;

    /**
     * Description: Number of parameters required for the error model chosen.
     * Units: none
     */
    public int num_error_model_params;

    /**
     * Description: Error model parameters that characterize the optional error model.
     * Units: various
     * LCM Type: double[num_error_model_params]
     */
    public double error_model_params[];

    /**
     * Description: Number of integrity values.
     * Units: none
     */
    public short num_integrity;

    /**
     * Description: Measurement integrity. Includes the integrity method used and an integrity value
     * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
     * 1 is to report multiple integrity values based on multiple integrity methods.
     * Units: none
     * LCM Type: aspn23_lcm.type_integrity[num_integrity]
     */
    public aspn23_lcm.type_integrity integrity[];

 
    public type_direction_3d_to_point()
    {
        obs = new double[2];
        covariance = new double[2][2];
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x8a23993728bb9489L;
 
    /**
     * Azimuth (first component of obs) is the angle between the sensor frame x-axis (boresight) and
     * the projection of the point onto the sensor frame's x-y plane (positive right-hand rule about the
     * sensor frame z-axis). Elevation (second component of obs) is the angle between the sensor frame's
     * x-y plane and the vector from the sensor to the point (positive right-hand rule about the
     * intermediate axis formed by the azimuth rotation). The covariance is for the azimuth and elevation
     * errors. Units for the observation are in rad, covariance rad^2.
     */
    public static final byte REFERENCE_FRAME_AZ_EL = (byte) 0;
    /**
     * The pixel coordinates of the point in an image (sub-pixel resolution is allowed). When combined
     * with the camera model, these can be converted to the NORMALIZED_IMAGE reference frame
     * representation. Units for the observation are in pixels, covariance pixels^2.
     */
    public static final byte REFERENCE_FRAME_PIXEL = (byte) 1;
    /**
     * 2D position from the sensor frame origin to the point expressed in components y and z of the
     * sensor frame normalized by the boresight distance. The x component of the sensor frame is the
     * boresight axis. Units for the observation are m/m, covariance (m/m)^2.
     */
    public static final byte REFERENCE_FRAME_NORMALIZED_IMAGE = (byte) 2;
    /**
     * 2D position from the sensor frame origin to the point expressed in components y and z of the
     * sensor frame normalized by the range to the point. The x component of the sensor frame is the
     * boresight axis. Units for the observation are m/m, covariance (m/m)^2.
     */
    public static final byte REFERENCE_FRAME_SINE_SPACE = (byte) 3;
    /**
     * No additional error model provided (num_error_model_params = 0).
     */
    public static final byte ERROR_MODEL_NONE = (byte) 0;

    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(aspn23_lcm.type_direction_3d_to_point.class))
            return 0L;
 
        classes.add(aspn23_lcm.type_direction_3d_to_point.class);
        long hash = LCM_FINGERPRINT_BASE
             + aspn23_lcm.type_remote_point._hashRecursive(classes)
             + aspn23_lcm.type_image_feature._hashRecursive(classes)
             + aspn23_lcm.type_integrity._hashRecursive(classes)
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        outs.writeByte(this.icd_type_direction_3d_to_point); 
 
        this.remote_point._encodeRecursive(outs); 
 
        outs.writeByte(this.reference_frame); 
 
        for (int a = 0; a < 2; a++) {
            outs.writeDouble(this.obs[a]); 
        }
 
        for (int a = 0; a < 2; a++) {
            for (int b = 0; b < 2; b++) {
                outs.writeDouble(this.covariance[a][b]); 
            }
        }
 
        outs.writeByte( this.has_observation_characteristics ? 1 : 0); 
 
        this.observation_characteristics._encodeRecursive(outs); 
 
        outs.writeByte(this.error_model); 
 
        outs.writeInt(this.num_error_model_params); 
 
        for (int a = 0; a < this.num_error_model_params; a++) {
            outs.writeDouble(this.error_model_params[a]); 
        }
 
        outs.writeShort(this.num_integrity); 
 
        for (int a = 0; a < this.num_integrity; a++) {
            this.integrity[a]._encodeRecursive(outs); 
        }
 
    }
 
    public type_direction_3d_to_point(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public type_direction_3d_to_point(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static aspn23_lcm.type_direction_3d_to_point _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        aspn23_lcm.type_direction_3d_to_point o = new aspn23_lcm.type_direction_3d_to_point();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.icd_type_direction_3d_to_point = ins.readByte();
 
        this.remote_point = aspn23_lcm.type_remote_point._decodeRecursiveFactory(ins);
 
        this.reference_frame = ins.readByte();
 
        this.obs = new double[(int) 2];
        for (int a = 0; a < 2; a++) {
            this.obs[a] = ins.readDouble();
        }
 
        this.covariance = new double[(int) 2][(int) 2];
        for (int a = 0; a < 2; a++) {
            for (int b = 0; b < 2; b++) {
                this.covariance[a][b] = ins.readDouble();
            }
        }
 
        this.has_observation_characteristics = ins.readByte()!=0;
 
        this.observation_characteristics = aspn23_lcm.type_image_feature._decodeRecursiveFactory(ins);
 
        this.error_model = ins.readByte();
 
        this.num_error_model_params = ins.readInt();
 
        this.error_model_params = new double[(int) num_error_model_params];
        for (int a = 0; a < this.num_error_model_params; a++) {
            this.error_model_params[a] = ins.readDouble();
        }
 
        this.num_integrity = ins.readShort();
 
        this.integrity = new aspn23_lcm.type_integrity[(int) num_integrity];
        for (int a = 0; a < this.num_integrity; a++) {
            this.integrity[a] = aspn23_lcm.type_integrity._decodeRecursiveFactory(ins);
        }
 
    }
 
    public aspn23_lcm.type_direction_3d_to_point copy()
    {
        aspn23_lcm.type_direction_3d_to_point outobj = new aspn23_lcm.type_direction_3d_to_point();
        outobj.icd_type_direction_3d_to_point = this.icd_type_direction_3d_to_point;
 
        outobj.remote_point = this.remote_point.copy();
 
        outobj.reference_frame = this.reference_frame;
 
        outobj.obs = new double[(int) 2];
        System.arraycopy(this.obs, 0, outobj.obs, 0, 2); 
        outobj.covariance = new double[(int) 2][(int) 2];
        for (int a = 0; a < 2; a++) {
            System.arraycopy(this.covariance[a], 0, outobj.covariance[a], 0, 2);        }
 
        outobj.has_observation_characteristics = this.has_observation_characteristics;
 
        outobj.observation_characteristics = this.observation_characteristics.copy();
 
        outobj.error_model = this.error_model;
 
        outobj.num_error_model_params = this.num_error_model_params;
 
        outobj.error_model_params = new double[(int) num_error_model_params];
        if (this.num_error_model_params > 0)
            System.arraycopy(this.error_model_params, 0, outobj.error_model_params, 0, (int) this.num_error_model_params); 
        outobj.num_integrity = this.num_integrity;
 
        outobj.integrity = new aspn23_lcm.type_integrity[(int) num_integrity];
        for (int a = 0; a < this.num_integrity; a++) {
            outobj.integrity[a] = this.integrity[a].copy();
        }
 
        return outobj;
    }
 
}

