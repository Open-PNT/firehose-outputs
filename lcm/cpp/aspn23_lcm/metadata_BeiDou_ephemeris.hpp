/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_metadata_BeiDou_ephemeris_hpp__
#define __aspn23_lcm_metadata_BeiDou_ephemeris_hpp__

#include <lcm/lcm_coretypes.h>

#include "aspn23_lcm/type_metadataheader.hpp"
#include "aspn23_lcm/type_timestamp.hpp"
#include "aspn23_lcm/type_satnav_clock.hpp"
#include "aspn23_lcm/type_kepler_orbit.hpp"

namespace aspn23_lcm
{

/**
 * BeiDou Ephemeris describing satellite locations. Definitions and usage are covered in
 * BDS-SIS-ICD-BII-3.0, February 2019,
 */
class metadata_BeiDou_ephemeris
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_metadata_BeiDou_ephemeris;

        /**
         * Description: Standard ASPN metadata header.
         * Units: none
         */
        aspn23_lcm::type_metadataheader info;

        /**
         * Description: Time at which the measurement is considered to be valid.
         * Units: none
         */
        aspn23_lcm::type_timestamp time_of_validity;

        /**
         * Description: Satellite PRN number.
         * Units: none
         */
        int32_t    prn;

        /**
         * Description: GNSS broadcast parameters required to calculate sv clock corrections.
         * Units: none
         */
        aspn23_lcm::type_satnav_clock clock;

        /**
         * Description: Keplerian orbit parameters required to calculate satellite position.
         * Units: none
         */
        aspn23_lcm::type_kepler_orbit orbit;

        /**
         * Description: Group delay differential for B1I user.
         * Units: s
         */
        double     t_gd1;

        /**
         * Description: Group delay differential for B2I user.
         * Units: s
         */
        double     t_gd2;

        /**
         * Description: Age of data, clock is updated at start of each hour in BDT per table 5-6 in
         * BDS-SIS-ICD-BII-3.0.
         * Units: none
         */
        int16_t    aodc;

        /**
         * Description: Age of data, ephemeris is updated at start of each hour in BDT per table 5-8 in
         * BDS-SIS-ICD-BII-3.0.
         * Units: none
         */
        int16_t    aode;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "metadata_BeiDou_ephemeris"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int metadata_BeiDou_ephemeris::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int metadata_BeiDou_ephemeris::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int metadata_BeiDou_ephemeris::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t metadata_BeiDou_ephemeris::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* metadata_BeiDou_ephemeris::getTypeName()
{
    return "metadata_BeiDou_ephemeris";
}

int metadata_BeiDou_ephemeris::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_metadata_BeiDou_ephemeris, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->info._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->prn, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->clock._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->orbit._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->t_gd1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->t_gd2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->aodc, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->aode, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int metadata_BeiDou_ephemeris::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_metadata_BeiDou_ephemeris, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->info._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->prn, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->clock._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->orbit._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->t_gd1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->t_gd2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->aodc, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->aode, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int metadata_BeiDou_ephemeris::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->info._getEncodedSizeNoHash();
    enc_size += this->time_of_validity._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += this->clock._getEncodedSizeNoHash();
    enc_size += this->orbit._getEncodedSizeNoHash();
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t metadata_BeiDou_ephemeris::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == metadata_BeiDou_ephemeris::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, metadata_BeiDou_ephemeris::getHash };

    uint64_t hash = 0xd9732d27913fec47LL +
         aspn23_lcm::type_metadataheader::_computeHash(&cp) +
         aspn23_lcm::type_timestamp::_computeHash(&cp) +
         aspn23_lcm::type_satnav_clock::_computeHash(&cp) +
         aspn23_lcm::type_kepler_orbit::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
