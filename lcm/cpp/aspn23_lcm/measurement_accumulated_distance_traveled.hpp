/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_measurement_accumulated_distance_traveled_hpp__
#define __aspn23_lcm_measurement_accumulated_distance_traveled_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "aspn23_lcm/type_header.hpp"
#include "aspn23_lcm/type_timestamp.hpp"
#include "aspn23_lcm/type_integrity.hpp"

namespace aspn23_lcm
{

/**
 * Accumulated distance traveled over the time period from (time_of_validity - delta_t) to
 * time_of_validity. For example, a sensor that travels (over the observation time period) in one
 * direction 1m and reverses course to travel 1m back (and ends at its starting position) would have an
 * accumulated distance traveled of 2m. Sensor examples may include an odometer or a wheel encoder.
 */
class measurement_accumulated_distance_traveled
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_measurement_accumulated_distance_traveled;

        /**
         * Description: Standard ASPN measurement header.
         * Units: none
         */
        aspn23_lcm::type_header header;

        /**
         * Description: Time at which the measurement is considered to be valid.
         * Units: none
         */
        aspn23_lcm::type_timestamp time_of_validity;

        /**
         * Description: Duration of the observation time interval. The time_of_validity denotes the end of
         * the time interval. The start of the time interval is time_of_validity - delta_t.
         * Units: sec
         */
        double     delta_t;

        /**
         * Description: Accumulated distance traveled over time period.
         * Units: m
         */
        double     obs;

        /**
         * Description: Accumulated distance traveled variance.
         * Units: m^2
         */
        double     variance;

        /**
         * Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).
         */
        int8_t     error_model;

        /**
         * Description: Number of parameters required for the error model chosen.
         * Units: none
         */
        int32_t    num_error_model_params;

        /**
         * Description: Error model parameters that characterize the optional error model.
         * Units: various
         * LCM Type: double[num_error_model_params]
         */
        std::vector< double > error_model_params;

        /**
         * Description: Number of integrity values.
         * Units: none
         */
        int16_t    num_integrity;

        /**
         * Description: Measurement integrity. Includes the integrity method used and an integrity value
         * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
         * 1 is to report multiple integrity values based on multiple integrity methods.
         * Units: none
         * LCM Type: aspn23_lcm.type_integrity[num_integrity]
         */
        std::vector< aspn23_lcm::type_integrity > integrity;

    public:
        /// No additional error model provided (num_error_model_params = 0).
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   ERROR_MODEL_NONE = 0;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "measurement_accumulated_distance_traveled"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int measurement_accumulated_distance_traveled::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int measurement_accumulated_distance_traveled::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int measurement_accumulated_distance_traveled::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t measurement_accumulated_distance_traveled::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* measurement_accumulated_distance_traveled::getTypeName()
{
    return "measurement_accumulated_distance_traveled";
}

int measurement_accumulated_distance_traveled::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_measurement_accumulated_distance_traveled, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->delta_t, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->obs, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->error_model, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_error_model_params, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_error_model_params > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->error_model_params[0], this->num_error_model_params);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int measurement_accumulated_distance_traveled::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_measurement_accumulated_distance_traveled, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->delta_t, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->obs, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->error_model, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_error_model_params, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_error_model_params) {
        this->error_model_params.resize(this->num_error_model_params);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->error_model_params[0], this->num_error_model_params);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->integrity.resize(this->num_integrity);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int measurement_accumulated_distance_traveled::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += this->time_of_validity._getEncodedSizeNoHash();
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, this->num_error_model_params);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        enc_size += this->integrity[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t measurement_accumulated_distance_traveled::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == measurement_accumulated_distance_traveled::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, measurement_accumulated_distance_traveled::getHash };

    uint64_t hash = 0xc2db24073d386ac4LL +
         aspn23_lcm::type_header::_computeHash(&cp) +
         aspn23_lcm::type_timestamp::_computeHash(&cp) +
         aspn23_lcm::type_integrity::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
