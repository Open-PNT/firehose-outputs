/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_type_satnav_obs_hpp__
#define __aspn23_lcm_type_satnav_obs_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "aspn23_lcm/type_satnav_satellite_system.hpp"
#include "aspn23_lcm/type_satnav_signal_descriptor.hpp"
#include "aspn23_lcm/type_integrity.hpp"

namespace aspn23_lcm
{

/**
 * Defines measurements that come from a satnav receiver tracking a single signal from a single
 * satellite.
 */
class type_satnav_obs
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_type_satnav_obs;

        /**
         * Description: Enumerated field which describes the Satellite System that generated the
         * observations.
         * Units: none
         */
        aspn23_lcm::type_satnav_satellite_system satellite_system;

        /**
         * Description: Satellite signal descriptor as defined in RINEX 3.05
         * Units: none
         */
        aspn23_lcm::type_satnav_signal_descriptor signal_descriptor;

        /**
         * Description: PRN code which identifies satellite (or slot number, in the case of GLONASS) for
         * the observables.
         * Units: none
         */
        int16_t    prn;

        /**
         * Description: Center frequency of signal. For GLONASS, this should be the center frequency of the
         * frequency-shifted signal with the k value taken into account.
         * Units: Hz
         */
        double     frequency;

        /**
         * Description: Pseudorange measurement
         * Units: m
         */
        double     pseudorange;

        /**
         * Description: Variance of pseudorange measurement noise/multipath. This value must be provided if
         * the pseudorange measurement is provided. Note: This is not intended to represent clock, atmospheric,
         * or satellite position errors.
         * Units: m^2
         */
        float      pseudorange_variance;

        /**
         * Indicates the type of measurement contained in pseudorange_rate.
         */
        int8_t     pseudorange_rate_type;

        /**
         * Description: Measurement of the change in the pseudorange. See pseudorange_rate_type for
         * details.
         * Units: Hz or m
         */
        double     pseudorange_rate;

        /**
         * Description: Variance of pseudorange_rate noise. This value must be provided if the
         * pseudorange_rate measurement is provided.
         * Units: Hz^2 or m^2
         */
        float      pseudorange_rate_variance;

        /**
         * Description: Carrier-phase (integrated Doppler) measurement. This measurement has an N cycle
         * ambiguity (where N is unknown and arbitrary integer).
         * Units: cycles (1 cycle = wavelength of carrier frequency)
         */
        double     carrier_phase;

        /**
         * Description: Variance of carrier-phase noise/multipath. This value must be provided if the
         * carrier-phase measurement is provided. Note: This is not intended to represent clock, atmospheric,
         * or satellite position errors or the integer N ambiguity.
         * Units: cycles^2
         */
        float      carrier_phase_variance;

        /**
         * Description: Carrier to noise density ratio. C/N0 = 10[log10(S/N0)]
         * Units: db-Hz
         */
        float      c_n0;

        /**
         * Description: Number of observations with continuous tracking (no cycle slips). Value of -1 means
         * that there is no loss of lock information available. First lock_count after cycle slip has occurred
         * will have a value of 0. The lock_count only applies to the carrier-phase measurement.
         * Units: none
         */
        int32_t    lock_count;

        /**
         * Enumerated field which describes the source of the ionospheric delay correction.
         */
        int8_t     iono_correction_source;

        /**
         * Description: Identifies if the ionospheric correction has been applied to the measurement. 1 =
         * Applied.
         * Units: none
         */
        int8_t     iono_correction_applied;

        /**
         * Description: Identifies if the tropospheric correction has been applied to the measurement. 1 =
         * Applied.
         * Units: none
         */
        int8_t     tropo_correction_applied;

        /**
         * Description: Represents biases within the transmitting signal that are identified in the
         * downlink data, Group Delay for LNAV, and Interchannel Signal Correction (ISC) message for MNAV. This
         * term indicates whether the correction is known, being applied to Pseudorange Correction, and its
         * associated estimated error applied to the ERE. When invalid the correction and estimated error are
         * not known and therefore not applied. 1 = Valid.
         * Units: none
         */
        int8_t     signal_bias_correction_applied;

        /**
         * Description: Number of integrity values.
         * Units: none
         */
        int16_t    num_integrity;

        /**
         * Description: Measurement integrity. Includes the integrity method used and an integrity value
         * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
         * 1 is to report multiple integrity values based on multiple integrity methods.
         * Units: none
         * LCM Type: aspn23_lcm.type_integrity[num_integrity]
         */
        std::vector< aspn23_lcm::type_integrity > integrity;

    public:
        /// Instantaneous Doppler at measurement time. Units = Hz Cov. Units = Hz^2
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   PSEUDORANGE_RATE_TYPE_PSR_RATE_DOPPLER = 0;
        /**
         * Change in pseudorange (integrated Doppler) over a specified time interval. The interval is
         * provided in metadata_satnav_obs. Units = m Cov. Units = m^2
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   PSEUDORANGE_RATE_TYPE_PSR_RATE_DELTA_RANGE = 1;
        /// unknown source
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   IONO_CORRECTION_SOURCE_UNKNOWN = 0;
        /// modeled using the ionospheric models in IS-GPS-200, IS-GPS-700, or IS-GPS-800
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   IONO_CORRECTION_SOURCE_MODELED = 1;
        /// measured using dual frequencies
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   IONO_CORRECTION_SOURCE_MEASURED = 2;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "type_satnav_obs"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int type_satnav_obs::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_satnav_obs::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int type_satnav_obs::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t type_satnav_obs::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* type_satnav_obs::getTypeName()
{
    return "type_satnav_obs";
}

int type_satnav_obs::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_type_satnav_obs, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->satellite_system._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->signal_descriptor._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->prn, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->frequency, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->pseudorange, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_rate_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_rate, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_rate_variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->carrier_phase, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->carrier_phase_variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->c_n0, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->lock_count, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->iono_correction_source, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->iono_correction_applied, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->tropo_correction_applied, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->signal_bias_correction_applied, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int type_satnav_obs::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_type_satnav_obs, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->satellite_system._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->signal_descriptor._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->prn, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->frequency, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->pseudorange, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_rate_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_rate, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->pseudorange_rate_variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->carrier_phase, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->carrier_phase_variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->c_n0, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->lock_count, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->iono_correction_source, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->iono_correction_applied, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->tropo_correction_applied, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->signal_bias_correction_applied, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->integrity.resize(this->num_integrity);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int type_satnav_obs::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->satellite_system._getEncodedSizeNoHash();
    enc_size += this->signal_descriptor._getEncodedSizeNoHash();
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        enc_size += this->integrity[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t type_satnav_obs::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == type_satnav_obs::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, type_satnav_obs::getHash };

    uint64_t hash = 0xa9ab728c73458babLL +
         aspn23_lcm::type_satnav_satellite_system::_computeHash(&cp) +
         aspn23_lcm::type_satnav_signal_descriptor::_computeHash(&cp) +
         aspn23_lcm::type_integrity::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
