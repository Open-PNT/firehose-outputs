/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_type_metadataheader_hpp__
#define __aspn23_lcm_type_metadataheader_hpp__

#include <lcm/lcm_coretypes.h>

#include <string>
#include "aspn23_lcm/type_header.hpp"

namespace aspn23_lcm
{

/// Sensor metadata header.
class type_metadataheader
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_type_metadataheader;

        /**
         * Description: Standard ASPN measurement header.
         * Units: none
         */
        aspn23_lcm::type_header header;

        /**
         * Description: Describes the device used to generate the measurement (user defined).
         * Units: none
         */
        std::string sensor_description;

        /**
         * Description: Optional nominal time interval between each measurement. Actual time interval may
         * vary. Do not report a delta_t_nom if the nominal time interval is not known ahead of time.
         * Units: seconds
         */
        double     delta_t_nom;

        /**
         * Description: Identifier for the timestamp's clock/timing source. See clock_identifiers.md for a
         * full description, but the summary is as follows:
         * 0 = ASPN System Time
         * 1 = International Atomic Time (TAI)
         * 2 = Universal Coordinated Time (UTC)
         * 3 = GPS System Time
         * 4 = Galileo System Time
         * 5 = GLONASS System Time
         * 6 = BeiDou System Time
         * 7-50: Reserved for future additional time scale representations.
         *
         * Each message source must provide metadata information (the method used to provide the metadata
         * information depends on the system, e.g., over the wire, written to file, etc.), and the information
         * contained in this type (type_metadataheader) will be included in that information. In this manner,
         * each message source's clock ID will be defined. If the clock source is updated, new metadata must be
         * provided with an updated clock ID.
         * If used, ASPN system time shall be represented as a monotonically increasing quantity defined by
         * the system. System time zero epoch must be defined. For example, system time may be the time
         * difference with respect to a system power on event. Other user-defined clocks/timing sources must be
         * similarly defined by the system designer.
         * Units: none
         */
        int16_t    timestamp_clock_id;

        /**
         * Description: Defines how many decimal digits of precision are represented in the timestamp after
         * the decimal point. The precision of the timestamps is 10E-N, where N is digits_of_precision. A value
         * of 3 would mean that the timestamp has precision at the 10E-3 (millisecond) level, a value of 6
         * would mean the timestamp has precision at the 10E-6 (microsecond) level, etc.
         * If an implementation is using 64bit integers for their internal time representations, digits of
         * precision may be ignored with no additional complexity. If an implementation is converting to a
         * double, because of their choice of internal representation (which likely most estimators would use),
         * then knowing digits of precision would be important to avoid losing resolution.
         * Units: none
         */
        int16_t    digits_of_precision;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "type_metadataheader"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int type_metadataheader::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_metadataheader::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int type_metadataheader::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t type_metadataheader::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* type_metadataheader::getTypeName()
{
    return "type_metadataheader";
}

int type_metadataheader::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_type_metadataheader, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    char* sensor_description_cstr = const_cast<char*>(this->sensor_description.c_str());
    tlen = __string_encode_array(
        buf, offset + pos, maxlen - pos, &sensor_description_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->delta_t_nom, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->timestamp_clock_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->digits_of_precision, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_metadataheader::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_type_metadataheader, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __sensor_description_len__;
    tlen = __int32_t_decode_array(
        buf, offset + pos, maxlen - pos, &__sensor_description_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__sensor_description_len__ > maxlen - pos) return -1;
    this->sensor_description.assign(
        static_cast<const char*>(buf) + offset + pos, __sensor_description_len__ - 1);
    pos += __sensor_description_len__;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->delta_t_nom, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->timestamp_clock_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->digits_of_precision, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_metadataheader::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += this->sensor_description.size() + 4 + 1;
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t type_metadataheader::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == type_metadataheader::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, type_metadataheader::getHash };

    uint64_t hash = 0xe9550c5c6e6fe245LL +
         aspn23_lcm::type_header::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
