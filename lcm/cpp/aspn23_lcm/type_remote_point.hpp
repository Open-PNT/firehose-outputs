/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_type_remote_point_hpp__
#define __aspn23_lcm_type_remote_point_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>

namespace aspn23_lcm
{

/**
 * ASPN custom type representing a point. This includes optional information
 * (indicated by included_terms) for the position and a correspondence ID for the
 * point.
 */
class type_remote_point
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_type_remote_point;

        /**
         * Description: Indicates which information is included about the point
         * 0 = no information included (unknown point)
         * 1 = id included (corresponded point)
         * 2 = position included (known point)
         * 3 = id and position included
         * Units: none
         */
        int16_t    included_terms;

        /**
         * Description: Unique identification number assigned by the sensor. Points that have the same id
         * from a sensor are multiple instances of the same point as determined by a sensor's point
         * correspondence.
         * Units: none
         */
        int64_t    id;

        /**
         * Defines position reference.
         */
        int8_t     position_reference_frame;

        /**
         * Description: First position term as defined in position_reference_frame.
         * Units: various
         */
        double     position1;

        /**
         * Description: Second position term as defined in position_reference_frame.
         * Units: various
         */
        double     position2;

        /**
         * Description: Third position term as defined in position_reference_frame.
         * Units: various
         */
        double     position3;

        /**
         * Description: Dimension of position provided.
         * Units: none
         */
        int16_t    num_position_components;

        /**
         * Description: Position error covariance (or variance depending on num_position_components) as
         * defined in position_reference_frame.
         * Units: m^2
         * LCM Type: double[num_position_components][num_position_components]
         */
        std::vector< std::vector< double > > position_covariance;

    public:
        /**
         * No reference frame. This is only valid if position is not included
         * (included_terms is 0 or 1).
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   POSITION_REFERENCE_FRAME_NONE = 0;
        /**
         * Point position relative to the WGS-84 reference expressed using position1 as geodetic latitude
         * in radians, position2 as longitude in radians, and position3 as geodetic altitude (or height
         * above the WGS-84 ellipsoid) in meters. Each term is optional.
         * position_covariance is given using north-south position error in meters (an expression of
         * geodetic latitude error), east-west position error in meters (an expression of longitude
         * error), and geodetic altitude error in meters. In the case of nulled position components, the
         * corresponding error covariance terms are omitted.
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   POSITION_REFERENCE_FRAME_GEODETIC = 1;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "type_remote_point"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int type_remote_point::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_remote_point::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int type_remote_point::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t type_remote_point::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* type_remote_point::getTypeName()
{
    return "type_remote_point";
}

int type_remote_point::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_type_remote_point, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->included_terms, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->position_reference_frame, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->position1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->position2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->position3, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_position_components, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_position_components > 0) {
        for (int a0 = 0; a0 < this->num_position_components; a0++) {
            tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->position_covariance[a0][0], this->num_position_components);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    return pos;
}

int type_remote_point::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_type_remote_point, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->included_terms, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->position_reference_frame, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->position1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->position2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->position3, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_position_components, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->position_covariance.resize(this->num_position_components);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_position_components; a0++) {
        if(this->num_position_components) {
            this->position_covariance[a0].resize(this->num_position_components);
            tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->position_covariance[a0][0], this->num_position_components);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    return pos;
}

int type_remote_point::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += this->num_position_components * __double_encoded_array_size(NULL, this->num_position_components);
    return enc_size;
}

uint64_t type_remote_point::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0x985107f99d375eb5LL;
    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
