/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_measurement_position_velocity_attitude_hpp__
#define __aspn23_lcm_measurement_position_velocity_attitude_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "aspn23_lcm/type_header.hpp"
#include "aspn23_lcm/type_timestamp.hpp"
#include "aspn23_lcm/type_integrity.hpp"

namespace aspn23_lcm
{

/**
 * Position, velocity, and attitude. Position, in whole or in part, is optional. Velocity, in whole or
 * in part, is optional. Attitude is optional, but if included, must be 3-dimensional. Position is
 * relative to a user-defined reference frame. Velocity is time rate of change of position with respect
 * to the user-specified reference frame. Attitude expressed as a quaternion, a four element vector
 * representation, the elements of which are functions of the orientation of the vector and the
 * magnitude of the rotation.
 */
class measurement_position_velocity_attitude
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_measurement_position_velocity_attitude;

        /**
         * Description: Standard ASPN measurement header.
         * Units: none
         */
        aspn23_lcm::type_header header;

        /**
         * Description: Time at which the measurement is considered to be valid.
         * Units: none
         */
        aspn23_lcm::type_timestamp time_of_validity;

        /**
         * Defines measurement reference.
         */
        int8_t     reference_frame;

        /**
         * Description: First position term as defined in enum.
         * Units: various
         */
        double     p1;

        /**
         * Description: Second position term as defined in enum.
         * Units: various
         */
        double     p2;

        /**
         * Description: Third position term as defined in enum.
         * Units: m
         */
        double     p3;

        /**
         * Description: Velocity along the first axis of the measurement reference frame enumerated in
         * reference_frame.
         * Units: m/s
         */
        double     v1;

        /**
         * Description: Velocity along the second axis of the measurement reference frame enumerated in
         * reference_frame.
         * Units: m/s
         */
        double     v2;

        /**
         * Description: Velocity along the third axis of the measurement reference frame enumerated in
         * reference_frame.
         * Units: m/s
         */
        double     v3;

        /**
         * Description: Four element quaternion, q = [a, b, c, d], where a = cos(phi/2), b =
         * (phi_x/phi)*sin(phi/2), c = (phi_y/phi)*sin(phi/2), and d = (phi_z/phi)*sin(phi/2). In this
         * description, the vector [phi_x, phi_y, phi_z] represents the rotation vector that describes the
         * frame rotation to be applied to the "reference" frame (ECI, ECEF, or NED) to rotate it into the axes
         * that describe the measured attitude, and the value phi is the magnitude of the [phi_x, phi_y, phi_z]
         * vector.
         * See "conventions" documentation for more detailed information.
         * Units: none
         * LCM Type: double[4]
         */
        double     quaternion[4];

        /**
         * Description: Dimension of measurement provided. In this sense, num_meas is the number of
         * measurement terms that include valid data.
         * Units: none
         */
        int16_t    num_meas;

        /**
         * Description: Measurement error variance or covariance depending on measurement dimension. NOTE:
         * Attitude errors are expressed as tilt errors, so if an attitude is provided, that should count as 3
         * terms in num_meas. For example, a 3-D position, 3-D velocity, and attitude expressed as a quaternion
         * (which takes 10 terms total to describe) is num_meas = 9. A second example is a 3-D position and 3-D
         * velocity is num_meas = 6.
         * Units: various Dimensions of covariance must be num_meas²
         * LCM Type: double[num_meas][num_meas]
         */
        std::vector< std::vector< double > > covariance;

        /**
         * Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).
         */
        int8_t     error_model;

        /**
         * Description: Number of parameters required for the error model chosen.
         * Units: none
         */
        int32_t    num_error_model_params;

        /**
         * Description: Error model parameters that characterize the optional error model.
         * Units: various
         * LCM Type: double[num_error_model_params]
         */
        std::vector< double > error_model_params;

        /**
         * Description: Number of integrity values.
         * Units: none
         */
        int16_t    num_integrity;

        /**
         * Description: Measurement integrity. Includes the integrity method used and an integrity value
         * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
         * 1 is to report multiple integrity values based on multiple integrity methods.
         * Units: none
         * LCM Type: aspn23_lcm.type_integrity[num_integrity]
         */
        std::vector< aspn23_lcm::type_integrity > integrity;

    public:
        /**
         * Sensor position, velocity, and attitude relative to the earth-centered, inertial (ECi) frame.
         * Position is expressed where p1, p2, and p3 are the x, y, and z components of ECI position in meters,
         * respectively. Velocity is expressed where v1, v2, and v3 are the x, y, and z components of velocity
         * with respect to the ECi frame in m/s, respectively. Attitude is expressed as a four-element
         * quaternion. Position, in whole or in part, is optional. Velocity, in whole or in part, is optional.
         * Attitude is optional, but if included, must be 3 dimensional. Error covariance is given using x-axis
         * position error component (meters), y-axis position error component (meters), z-axis position error
         * component (meters), x-axis velocity error component (m/s), y-axis velocity error component (m/s),
         * z-axis velocity error component (m/s), tilt error about the x-axis (rad), tilt error about the
         * y-axis (rad), and tilt error about the z-axis (rad). In the case of nulled position, velocity, or
         * attitude, the corresponding error covariance terms are omitted. See also ECi coordinate frame
         * definition for x, y, and z definitions.
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   REFERENCE_FRAME_ECI = 0;
        /**
         * Sensor position, velocity, and attitude relative to the WGS-84 reference. Position is expressed
         * where p1 is the geodetic latitude in radians, p2 is the longitude in radians, and p3 is the geodetic
         * altitude (or height above the WGS-84 ellipsoid) in meters. Velocity with respect to the local level
         * geographical frame with the origin at the sensor position is expressed using the North-East-Down
         * (NED) convention, where v1 is the north velocity component in m/s, v2 is the east velocity component
         * in m/s, and v3 is the down velocity component in m/s. Attitude is the sensor frame rotation relative
         * to the local level frame expressed as a four-element quaternion. Position, in whole or in part, is
         * optional. Velocity, in whole or in part, is optional. Attitude is optional, but if included, must be
         * 3 dimensional. Error covariance is given using north-south position error (meters), east-west
         * position error (meters), geodetic altitude error (meters), north velocity component error (m/s),
         * east velocity component error (m/s), down velocity component error (m/s), tilt error about the north
         * axis (rad), tilt error about the east axis (rad), and tilt error about the down axis (rad). In the
         * case of nulled position, velocity, or attitude, the corresponding error covariance terms are
         * omitted.
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   REFERENCE_FRAME_GEODETIC = 1;
        /// No additional error model provided (num_error_model_params = 0).
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   ERROR_MODEL_NONE = 0;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "measurement_position_velocity_attitude"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int measurement_position_velocity_attitude::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int measurement_position_velocity_attitude::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int measurement_position_velocity_attitude::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t measurement_position_velocity_attitude::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* measurement_position_velocity_attitude::getTypeName()
{
    return "measurement_position_velocity_attitude";
}

int measurement_position_velocity_attitude::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_measurement_position_velocity_attitude, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->reference_frame, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->p1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->p2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->p3, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->v1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->v2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->v3, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->quaternion[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_meas, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_meas > 0) {
        for (int a0 = 0; a0 < this->num_meas; a0++) {
            tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->covariance[a0][0], this->num_meas);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->error_model, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_error_model_params, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_error_model_params > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->error_model_params[0], this->num_error_model_params);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int measurement_position_velocity_attitude::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_measurement_position_velocity_attitude, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->reference_frame, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->p1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->p2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->p3, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->v1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->v2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->v3, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->quaternion[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_meas, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->covariance.resize(this->num_meas);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_meas; a0++) {
        if(this->num_meas) {
            this->covariance[a0].resize(this->num_meas);
            tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->covariance[a0][0], this->num_meas);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->error_model, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_error_model_params, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_error_model_params) {
        this->error_model_params.resize(this->num_error_model_params);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->error_model_params[0], this->num_error_model_params);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->integrity.resize(this->num_integrity);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int measurement_position_velocity_attitude::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += this->time_of_validity._getEncodedSizeNoHash();
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 4);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += this->num_meas * __double_encoded_array_size(NULL, this->num_meas);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, this->num_error_model_params);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        enc_size += this->integrity[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t measurement_position_velocity_attitude::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == measurement_position_velocity_attitude::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, measurement_position_velocity_attitude::getHash };

    uint64_t hash = 0xf94f209bf89d3e2eLL +
         aspn23_lcm::type_header::_computeHash(&cp) +
         aspn23_lcm::type_timestamp::_computeHash(&cp) +
         aspn23_lcm::type_integrity::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
