/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_measurement_time_difference_hpp__
#define __aspn23_lcm_measurement_time_difference_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "aspn23_lcm/type_header.hpp"
#include "aspn23_lcm/type_timestamp.hpp"
#include "aspn23_lcm/type_integrity.hpp"

namespace aspn23_lcm
{

/// Time difference between two clocks or timing sources.
class measurement_time_difference
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_measurement_time_difference;

        /**
         * Description: Standard ASPN measurement header.
         * Units: none
         */
        aspn23_lcm::type_header header;

        /**
         * Description: Time at which the measurement is considered to be valid.
         * Units: none
         */
        aspn23_lcm::type_timestamp time_of_validity;

        /**
         * Description: Whole number of attoseconds used to provide additional timestamp fidelity
         * describing the time at which the measurement is considered to be valid. This is only needed if the
         * time of validity is known to more precision than 1ns; otherwise, set time_of_validity_attosec = 0.
         * Be sure to report the timestamp's digits of precision in the `type_metadataheader` as part of the
         * metadata.
         * The full timestamp in nanoseconds is calculated as
         *
         * time_of_validity + ( time_of_validity_attosec * 1E-9 )
         *
         * and if needed, a timestamp in seconds is calculated as
         *
         * time_of_validity * 1E-9 + time_of_validity_attosec * 1E-18
         *
         * (Recall time_of_validity is in nanoseconds and time_of_validity_attosec is in attoseconds.)
         * Furthermore, both time_of_validity and time_of_validity_attosec are signed integers and may be
         * positive or negative. It is recommended to make time_of_validity and time_of_validity_attosec the
         * same sign. Care should be taken when constructing the timestamp components to be sure the summation
         * produces the desired result.
         * Units: attoseconds
         */
        int32_t    time_of_validity_attosec;

        /**
         * Description: Identifier for clock/timing source 1. See clock_identifiers.md for a full
         * description, but the summary is as follows:
         * 0 = ASPN System Time
         * 1 = International Atomic Time (TAI)
         * 2 = Universal Coordinated Time (UTC)
         * 3 = GPS System Time
         * 4 = Galileo System Time
         * 5 = GLONASS System Time
         * 6 = BeiDou System Time
         * 7-50: Reserved for future additional time scale representations.
         * Units: none
         */
        int16_t    clock_id1;

        /**
         * Description: Identifier for clock/timing source 2. See clock_identifiers.md for a full
         * description, but the summary is as follows:
         * 0 = ASPN System Time
         * 1 = International Atomic Time (TAI)
         * 2 = Universal Coordinated Time (UTC)
         * 3 = GPS System Time
         * 4 = Galileo System Time
         * 5 = GLONASS System Time
         * 6 = BeiDou System Time
         * 7-50: Reserved for future additional time scale representations.
         * Units: none
         */
        int16_t    clock_id2;

        /**
         * Description: Whole number nanoseconds in time difference (time from clock_id1) - (time from
         * clock_id2). This value shall be positive when (time from clock_id1) - (time from clock_id2) is
         * greater than or equal to zero. This value shall be negative when (time from clock_id1) - (time from
         * clock_id2) is less than zero.Additionally, the timestamp's digits of precision are included as
         * `digits_of_precision`.
         * Units: nanoseconds
         */
        int64_t    time_diff_nsec;

        /**
         * Description: Whole number attoseconds in time difference (time from clock_id1) - (time from
         * clock_id2). Must be less than 1e9. This value shall be positive when (time from clock_id1) - (time
         * from clock_id2) is greater than or equal to zero. This value shall be negative when (time from
         * clock_id1) - (time from clock_id2) is less than zero.
         * This is only needed if the time of validity is known to more precision than 1ns; otherwise, set
         * time_of_validity_attosec = 0. Be sure to report the timestamp's digits of precision.
         * The time difference in seconds is calculated as
         *
         * time difference = time_diff_nsec * 1e-9 + time_diff_attosec * 1e-18
         *
         * It shall be assumed that time_diff_nsec and time_diff_attosec have the same sign. Therefore, the
         * time difference is positive when (time from clock_id1) - (time from clock_id2) is positive and the
         * time difference is negative when (time from clock_id1) - (time from clock_id2) is negative.
         * A cautionary note: Both timediff_nsec and time_diff_attosec are signed integers and may be
         * positive or negative. It is mandated to make timediff_nsec and time_diff_attosec the same sign;
         * however, care should be taken when constructing the observation components to be sure the summation
         * produces the desired result.
         * Units: attoseconds
         */
        int32_t    time_diff_attosec;

        /**
         * Description: Defines how many decimal digits of precision are represented in the observation
         * after the decimal point. The precision of the observation is 10E-N, where N is digits_of_precision.
         * A value of 3 would mean that the observation has precision at the 10E-3 (millisecond) level, a value
         * of 6 would mean the observation has precision at the 10E-6 (microsecond) level, etc.
         * Units: none
         */
        int16_t    digits_of_precision;

        /**
         * Description: Variance of the error in the time difference measurement.
         * Units: sec^2
         */
        double     variance;

        /**
         * Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).
         */
        int8_t     error_model;

        /**
         * Description: Number of parameters required for the error model chosen.
         * Units: none
         */
        int32_t    num_error_model_params;

        /**
         * Description: Error model parameters that characterize the optional error model.
         * Units: various
         * LCM Type: double[num_error_model_params]
         */
        std::vector< double > error_model_params;

        /**
         * Description: Number of integrity values.
         * Units: none
         */
        int16_t    num_integrity;

        /**
         * Description: Measurement integrity. Includes the integrity method used and an integrity value
         * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
         * 1 is to report multiple integrity values based on multiple integrity methods.
         * Units: none
         * LCM Type: aspn23_lcm.type_integrity[num_integrity]
         */
        std::vector< aspn23_lcm::type_integrity > integrity;

    public:
        /// No additional error model provided (num_error_model_params = 0).
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   ERROR_MODEL_NONE = 0;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "measurement_time_difference"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int measurement_time_difference::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int measurement_time_difference::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int measurement_time_difference::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t measurement_time_difference::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* measurement_time_difference::getTypeName()
{
    return "measurement_time_difference";
}

int measurement_time_difference::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_measurement_time_difference, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->time_of_validity_attosec, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->clock_id1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->clock_id2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->time_diff_nsec, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->time_diff_attosec, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->digits_of_precision, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->error_model, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_error_model_params, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_error_model_params > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->error_model_params[0], this->num_error_model_params);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int measurement_time_difference::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_measurement_time_difference, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->header._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->time_of_validity_attosec, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->clock_id1, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->clock_id2, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->time_diff_nsec, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->time_diff_attosec, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->digits_of_precision, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->variance, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->error_model, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_error_model_params, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_error_model_params) {
        this->error_model_params.resize(this->num_error_model_params);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->error_model_params[0], this->num_error_model_params);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_integrity, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->integrity.resize(this->num_integrity);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        tlen = this->integrity[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int measurement_time_difference::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->header._getEncodedSizeNoHash();
    enc_size += this->time_of_validity._getEncodedSizeNoHash();
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, this->num_error_model_params);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->num_integrity; a0++) {
        enc_size += this->integrity[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t measurement_time_difference::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == measurement_time_difference::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, measurement_time_difference::getHash };

    uint64_t hash = 0x3326bd85e25f62b9LL +
         aspn23_lcm::type_header::_computeHash(&cp) +
         aspn23_lcm::type_timestamp::_computeHash(&cp) +
         aspn23_lcm::type_integrity::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
