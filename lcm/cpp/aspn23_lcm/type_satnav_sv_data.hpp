/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_type_satnav_sv_data_hpp__
#define __aspn23_lcm_type_satnav_sv_data_hpp__

#include <lcm/lcm_coretypes.h>

#include "aspn23_lcm/type_satnav_satellite_system.hpp"
#include "aspn23_lcm/type_satnav_time.hpp"

namespace aspn23_lcm
{

/// Satellite position, velocity and clock error at a particular time epoch
class type_satnav_sv_data
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_type_satnav_sv_data;

        /**
         * Description: PRN code which identifies satellite (or slot number, in the case of GLONASS) of
         * this ephemeris.
         * Units: none
         */
        int16_t    prn;

        /**
         * Description: Describes the Satellite System which was used to generate the satellite information
         * provided in this message.
         * Units: none
         */
        aspn23_lcm::type_satnav_satellite_system satellite_system;

        /**
         * Provides further clarification of which ephemeris was used to generate the satellite information
         * provided in this message for systems which have more than one ephemeris representation.
         */
        int8_t     ephemeris_type;

        /**
         * Description: Receiver time at which the data provided in this message is valid.
         * Units: none
         */
        aspn23_lcm::type_satnav_time sv_data_time;

        /**
         * Coordinate frame system used to describe satellite position and velocity.
         */
        int8_t     coordinate_frame;

        /**
         * Description: Satellite position in frame specified by coordinate_frame at time specified by
         * sv_data_time_week_number and sv_data_time_seconds_of_week.
         * Units: m
         * LCM Type: double[3]
         */
        double     sv_pos[3];

        /**
         * Description: Satellite velocity in frame specified by coordinate_frame at time specified by
         * sv_data_time_week_number and sv_data_time_seconds_of_week.
         * Units: m/s
         * LCM Type: double[3]
         */
        double     sv_vel[3];

        /**
         * Description: Satellite clock bias at the sv_data_time, used to correct the satellite time like
         * delta_t_sv in equation (1) of ICD-GPS-200L, Section 20.3.3.3.3.1.
         * Units: sec
         */
        double     sv_clock_bias;

        /**
         * Description: Satellite clock drift rate at the sv_data_time. By way of example, for GPS this
         * would be af1 in equation (2) of ICD-GPS-200L, Section 20.3.3.3.3.1.
         * Units: sec/sec
         */
        double     sv_clock_drift;

        /**
         * Describes how to interpret group delay terms, because they vary by ephemeris type. Descriptions
         * assume zero-indexing.
         */
        int8_t     group_delay_enum;

        /**
         * Description: Group delay terms, with interpretation provided by group_delay_enum. (Need to
         * verify that four is sufficient for all ephemeris.)
         * Units: sec
         * LCM Type: float[4]
         */
        float      group_delay_vector[4];

    public:
        /**
         * To be used when a system only uses one ephemeris representation, so knowing the satellite_system
         * fully defines what ephemeris is being used.
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   EPHEMERIS_TYPE_SET_BY_SYSTEM = 0;
        /// Calculated using legacy GPS (LNAV) messages
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   EPHEMERIS_TYPE_GPS_LNAV = 1;
        /// Calculated using GPS CNAV messages
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   EPHEMERIS_TYPE_GPS_CNAV = 2;
        /// Calculated using GPS MNAV messages
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   EPHEMERIS_TYPE_GPS_MNAV = 3;
        /// International Terrestrial Reference Frame
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   COORDINATE_FRAME_ITRF = 0;
        /// Earth-centered earth-fixed frame as defined by WGS-84 (nearly identical with ITRF)
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   COORDINATE_FRAME_ECEF = 1;
        /// Galileo Terrestrial Reference Frame (nearly identical with ITRF)
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   COORDINATE_FRAME_GTRF = 2;
        /// Parametri Zemli 1990 (PZ-90) reference frame
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   COORDINATE_FRAME_PZ90 = 3;
        /**
         * group_delay_vector[0] is legacy Tgd as defined in ICD-GPS-200L Section 20.3.3.3.3.2. All other
         * terms not used.
         */
        // If you're using C++11 and are getting compiler errors saying
        // things like ‘constexpr’ needed for in-class initialization of
        // static data member then re-run lcm-gen with '--cpp-std=c++11'
        // to generate code that is compliant with C++11
        static const int8_t   GROUP_DELAY_ENUM_TGD_LNAV = 0;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "type_satnav_sv_data"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int type_satnav_sv_data::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_satnav_sv_data::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int type_satnav_sv_data::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t type_satnav_sv_data::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* type_satnav_sv_data::getTypeName()
{
    return "type_satnav_sv_data";
}

int type_satnav_sv_data::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_type_satnav_sv_data, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->prn, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->satellite_system._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->ephemeris_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->sv_data_time._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->coordinate_frame, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->sv_pos[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->sv_vel[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->sv_clock_bias, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->sv_clock_drift, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->group_delay_enum, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->group_delay_vector[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_satnav_sv_data::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_type_satnav_sv_data, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->prn, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->satellite_system._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->ephemeris_type, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->sv_data_time._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->coordinate_frame, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->sv_pos[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->sv_vel[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->sv_clock_bias, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->sv_clock_drift, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->group_delay_enum, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->group_delay_vector[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_satnav_sv_data::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += this->satellite_system._getEncodedSizeNoHash();
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->sv_data_time._getEncodedSizeNoHash();
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 3);
    enc_size += __double_encoded_array_size(NULL, 3);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 1);
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 4);
    return enc_size;
}

uint64_t type_satnav_sv_data::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == type_satnav_sv_data::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, type_satnav_sv_data::getHash };

    uint64_t hash = 0x6e0a9f4ab23eba20LL +
         aspn23_lcm::type_satnav_satellite_system::_computeHash(&cp) +
         aspn23_lcm::type_satnav_time::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
