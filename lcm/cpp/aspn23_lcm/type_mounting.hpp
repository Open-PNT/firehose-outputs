/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_type_mounting_hpp__
#define __aspn23_lcm_type_mounting_hpp__

#include <lcm/lcm_coretypes.h>


namespace aspn23_lcm
{

/**
 * Describes the translational and angular offset between the sensor frame and the platform body frame.
 * Platform body frame is defined using axes in forward, right, and down convention, and the origin is
 * system defined. This type also contains the uncertainty associated with the translational and
 * angular offset.
 */
class type_mounting
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_type_mounting;

        /**
         * Description: 3x1 lever arm vector describing the sensor position in the platform body frame.
         * Units: m
         * LCM Type: double[3]
         */
        double     lever_arm[3];

        /**
         * Description: 3x1 lever arm uncertainty vector as standard deviations in the platform body frame.
         * Units: m
         * LCM Type: double[3]
         */
        double     lever_arm_sigma[3];

        /**
         * Description: Four element quaternion, q = [a, b, c, d], where a = cos(phi/2), b =
         * (phi_x/phi)*sin(phi/2), c = (phi_y/phi)*sin(phi/2), and d = (phi_z/phi)*sin(phi/2). In this
         * description, the vector [phi_x, phi_y, phi_z] represents the rotation vector that describes the
         * frame rotation to be applied to the "reference" frame (ECI, ECEF, or NED) to rotate it into the axes
         * that describe the measured attitude, and the value phi is the magnitude of the [phi_x, phi_y, phi_z]
         * vector.
         * Orientation is optional in the sense that orientation shall be specified except in the case that
         * orientation is meaningless, for example, in the case of an RF antenna mounting.
         * See "conventions" documentation for more detailed information.
         * Units: none
         * LCM Type: double[4]
         */
        double     orientation_quaternion[4];

        /**
         * Description: Tilt error covariance matrix. This matrix represents the uncertainty in the "tilt
         * errors" that represent the additional rotation to be applied to the provided attitude quaternion in
         * order to convert it to the true attitude with no errors. By convention, these "tilt errors" are
         * expressed in the reference frame (ECI, ECEF, or NED).
         * Tilt error covariance is optional only because orientation itself is optional. Orientation error
         * covariance shall be provided if orientation is provided.
         * See "conventions" documentation for more detailed information.
         * Units: rad^2
         * LCM Type: double[3][3]
         */
        double     orientation_tilt_error_covariance[3][3];

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "type_mounting"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int type_mounting::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int type_mounting::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int type_mounting::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t type_mounting::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* type_mounting::getTypeName()
{
    return "type_mounting";
}

int type_mounting::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_type_mounting, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->lever_arm[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->lever_arm_sigma[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->orientation_quaternion[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < 3; a0++) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->orientation_tilt_error_covariance[a0][0], 3);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int type_mounting::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_type_mounting, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->lever_arm[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->lever_arm_sigma[0], 3);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->orientation_quaternion[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < 3; a0++) {
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->orientation_tilt_error_covariance[a0][0], 3);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int type_mounting::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += __double_encoded_array_size(NULL, 3);
    enc_size += __double_encoded_array_size(NULL, 3);
    enc_size += __double_encoded_array_size(NULL, 4);
    enc_size += 3 * __double_encoded_array_size(NULL, 3);
    return enc_size;
}

uint64_t type_mounting::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0xd0f86db27c5effbbLL;
    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
