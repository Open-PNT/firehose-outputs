/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen 1.5.2
 **/

/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef __aspn23_lcm_metadata_magnetic_field_hpp__
#define __aspn23_lcm_metadata_magnetic_field_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "aspn23_lcm/type_metadataheader.hpp"
#include "aspn23_lcm/type_timestamp.hpp"
#include "aspn23_lcm/type_mounting.hpp"

namespace aspn23_lcm
{

/**
 * Metadata for a magnetic field measurement. In addition to generic metadata information,
 * calibration parameters may be provided to account for effects such as soft iron, scale factor,
 * non-orthogonality, zero-bias, and hard iron. In general, for an num_meas-dimensional measurement,
 * the magnetic field calibration metadata (K and b) shall be used as
 *
 * m_calibrated = K * m_measured - b
 *
 * where m_calibrated, m_measured, and b are num_meas x 1 vectors (scalar for num_meas = 1) and K is
 * an num_meas x num_meas matrix (scalar for num_meas = 1). See magnetic_calibration.md for
 * additional details.
 *
 * More sophisticated approaches that include calibration parameters such as time-varying effects,
 * first-order Gauss-Markov bias models, and calibration parameter uncertainties may be included
 * using an appropriate error model in the measurement message.
 */
class metadata_magnetic_field
{
    public:
        /**
         * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
         */
        int8_t     icd_metadata_magnetic_field;

        /**
         * Description: Standard ASPN metadata header.
         * Units: none
         */
        aspn23_lcm::type_metadataheader info;

        /**
         * Description: Time at which the measurement is considered to be valid.
         * Units: none
         */
        aspn23_lcm::type_timestamp time_of_validity;

        /**
         * Description: Standard ASPN mounting information.
         * Units: none
         */
        aspn23_lcm::type_mounting mounting;

        /**
         * Description: Dimension of measurement for which this metadata is applicable. May be 1, 2, or 3.
         * Units: none
         */
        int16_t    num_meas;

        /**
         * Description: Optional calibration parameter to account for the combined effects of soft iron,
         * scale factor, and non-orthogonality as a unitless num_meas x num_meas matrix. Optional, but if
         * provided, b must also be provided.
         * Units: none
         * LCM Type: double[num_meas][num_meas]
         */
        std::vector< std::vector< double > > k;

        /**
         * Description: Optional calibration parameter to account for the combined effects of zero-bias and
         * hard iron as a num_meas x 1 vector in nanoTesla (nT). Optional, but if provided, K must also be
         * provided.
         * Units: nT
         * LCM Type: double[num_meas]
         */
        std::vector< double > b;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "metadata_magnetic_field"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int metadata_magnetic_field::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int metadata_magnetic_field::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int metadata_magnetic_field::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t metadata_magnetic_field::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* metadata_magnetic_field::getTypeName()
{
    return "metadata_magnetic_field";
}

int metadata_magnetic_field::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &this->icd_metadata_magnetic_field, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->info._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->mounting._encodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_meas, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_meas > 0) {
        for (int a0 = 0; a0 < this->num_meas; a0++) {
            tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->k[a0][0], this->num_meas);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    if(this->num_meas > 0) {
        tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->b[0], this->num_meas);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int metadata_magnetic_field::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &this->icd_metadata_magnetic_field, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->info._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->time_of_validity._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->mounting._decodeNoHash(buf, offset + pos, maxlen - pos);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_meas, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->k.resize(this->num_meas);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->num_meas; a0++) {
        if(this->num_meas) {
            this->k[a0].resize(this->num_meas);
            tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->k[a0][0], this->num_meas);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    if(this->num_meas) {
        this->b.resize(this->num_meas);
        tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->b[0], this->num_meas);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int metadata_magnetic_field::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int8_t_encoded_array_size(NULL, 1);
    enc_size += this->info._getEncodedSizeNoHash();
    enc_size += this->time_of_validity._getEncodedSizeNoHash();
    enc_size += this->mounting._getEncodedSizeNoHash();
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    enc_size += this->num_meas * __double_encoded_array_size(NULL, this->num_meas);
    enc_size += __double_encoded_array_size(NULL, this->num_meas);
    return enc_size;
}

uint64_t metadata_magnetic_field::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == metadata_magnetic_field::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, metadata_magnetic_field::getHash };

    uint64_t hash = 0xec9dbdee1877dc68LL +
         aspn23_lcm::type_metadataheader::_computeHash(&cp) +
         aspn23_lcm::type_timestamp::_computeHash(&cp) +
         aspn23_lcm::type_mounting::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
