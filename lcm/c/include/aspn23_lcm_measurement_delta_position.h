// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_measurement_delta_position_h
#define _aspn23_lcm_measurement_delta_position_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif

#include "aspn23_lcm_type_header.h"
#include "aspn23_lcm_type_timestamp.h"
#include "aspn23_lcm_type_integrity.h"

/**
 * Change in position along the N, E, and D axes of the locally level NED frame. Each direction is
 * optional. term1, term2, and term3, if provided, describe the change in position along the N, E, and
 * D axes, respectively. For example, a depth sensor may report a change in position of +2m along the D
 * axis by omitting term1 and term2 (using the appropriate null) and using term3 = 2. This observation
 * may be interpreted as (1) the sensor has no knowledge of the change in position in the N and E
 * directions, and (2) the sensor observed that the position at the end of the observation time
 * interval, time_of_validity, is 2 meters lower than the position at the start of the time interval,
 * time_of_validity - delta_t. See also NED coordinate frame definition for N, E, and D definitions.
 */
#define ASPN23_LCM_MEASUREMENT_DELTA_POSITION_REFERENCE_FRAME_NED 0

/**
 * Change in position expressed in the sensor frame at the start of the measurement interval as
 * defined in mounting. Each direction is optional. term1, term2, and term3, if provided, describe the
 * change in position along the sensor x, y, and z axes, respectively.
 */
#define ASPN23_LCM_MEASUREMENT_DELTA_POSITION_REFERENCE_FRAME_SENSOR_START 1

/**
 * Change in position expressed in the sensor frame at the end of the measurement interval as
 * defined in mounting. Each direction is optional. term1, term2, and term3, if provided, describe the
 * change in position along the sensor x, y, and z axes, respectively.
 */
#define ASPN23_LCM_MEASUREMENT_DELTA_POSITION_REFERENCE_FRAME_SENSOR_END 2
/// No additional error model provided (num_error_model_params = 0).
#define ASPN23_LCM_MEASUREMENT_DELTA_POSITION_ERROR_MODEL_NONE 0


/**
 * Delta position is the change in position from the position at time (time_of_validity - delta_t) to
 * the position at time (time_of_validity), where the change in position is reported along each axis of
 * the reference frame. The reference frame is chosen by the user in the reference enum.
 */
typedef struct _aspn23_lcm_measurement_delta_position aspn23_lcm_measurement_delta_position;
struct _aspn23_lcm_measurement_delta_position
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_measurement_delta_position;

    /**
     * Description: Standard ASPN measurement header.
     * Units: none
     */
    aspn23_lcm_type_header header;

    /**
     * Description: Time at which the measurement is considered to be valid at the end of the
     * delta-position period.
     * Units: none
     */
    aspn23_lcm_type_timestamp time_of_validity;

    /**
     * Specifies measurement reference frame.
     */
    int8_t     reference_frame;

    /**
     * Description: Duration of the observation time interval. The time_of_validity denotes the end of
     * the time interval. The start of the time interval is time_of_validity - delta_t.
     * Units: sec
     */
    double     delta_t;

    /**
     * Description: Dimension of measurement provided.
     * Units: none
     */
    int16_t    num_meas;

    /**
     * Description: Delta position over the first dimension specified by the enum.
     * Units: m
     */
    double     term1;

    /**
     * Description: Delta position over the second dimension specified by the enum.
     * Units: m
     */
    double     term2;

    /**
     * Description: Delta position over the third dimension specified by the enum.
     * Units: m
     */
    double     term3;

    /**
     * Description: Variance or covariance matrix depending on measurement dimension.
     * Units: m^2 Dimensions of covariance must be num_meas²
     * LCM Type: double[num_meas][num_meas]
     */
    double     **covariance;

    /**
     * Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).
     */
    int8_t     error_model;

    /**
     * Description: Number of parameters required for the error model chosen.
     * Units: none
     */
    int32_t    num_error_model_params;

    /**
     * Description: Error model parameters that characterize the optional error model.
     * Units: various
     * LCM Type: double[num_error_model_params]
     */
    double     *error_model_params;

    /**
     * Description: Number of integrity values.
     * Units: none
     */
    int16_t    num_integrity;

    /**
     * Description: Measurement integrity. Includes the integrity method used and an integrity value
     * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
     * 1 is to report multiple integrity values based on multiple integrity methods.
     * Units: none
     * LCM Type: aspn23_lcm.type_integrity[num_integrity]
     */
    aspn23_lcm_type_integrity *integrity;
};

/**
 * Create a deep copy of a aspn23_lcm_measurement_delta_position.
 * When no longer needed, destroy it with aspn23_lcm_measurement_delta_position_destroy()
 */
aspn23_lcm_measurement_delta_position* aspn23_lcm_measurement_delta_position_copy(const aspn23_lcm_measurement_delta_position* to_copy);

/**
 * Destroy an instance of aspn23_lcm_measurement_delta_position created by aspn23_lcm_measurement_delta_position_copy()
 */
void aspn23_lcm_measurement_delta_position_destroy(aspn23_lcm_measurement_delta_position* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_measurement_delta_position_subscription_t aspn23_lcm_measurement_delta_position_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_measurement_delta_position is received.
 */
typedef void(*aspn23_lcm_measurement_delta_position_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_measurement_delta_position *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_measurement_delta_position using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_measurement_delta_position_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_measurement_delta_position *msg);

/**
 * Subscribe to messages of type aspn23_lcm_measurement_delta_position using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_measurement_delta_position_subscription_t* aspn23_lcm_measurement_delta_position_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_measurement_delta_position_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_measurement_delta_position_subscribe()
 */
int aspn23_lcm_measurement_delta_position_unsubscribe(lcm_t *lcm, aspn23_lcm_measurement_delta_position_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_measurement_delta_position_subscription_set_queue_capacity(
    aspn23_lcm_measurement_delta_position_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_measurement_delta_position into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_measurement_delta_position_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_measurement_delta_position_encode(void *buf, int offset, int maxlen, const aspn23_lcm_measurement_delta_position *p);

/**
 * Decode a message of type aspn23_lcm_measurement_delta_position from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_measurement_delta_position_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_measurement_delta_position_decode(const void *buf, int offset, int maxlen, aspn23_lcm_measurement_delta_position *msg);

/**
 * Release resources allocated by aspn23_lcm_measurement_delta_position_decode()
 * @return 0
 */
int aspn23_lcm_measurement_delta_position_decode_cleanup(aspn23_lcm_measurement_delta_position *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_measurement_delta_position
 */
int aspn23_lcm_measurement_delta_position_encoded_size(const aspn23_lcm_measurement_delta_position *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_measurement_delta_position_get_hash(void);
uint64_t __aspn23_lcm_measurement_delta_position_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_measurement_delta_position_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_measurement_delta_position *p, int elements);
int __aspn23_lcm_measurement_delta_position_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_measurement_delta_position *p, int elements);
int __aspn23_lcm_measurement_delta_position_decode_array_cleanup(aspn23_lcm_measurement_delta_position *p, int elements);
int __aspn23_lcm_measurement_delta_position_encoded_array_size(const aspn23_lcm_measurement_delta_position *p, int elements);
int __aspn23_lcm_measurement_delta_position_clone_array(const aspn23_lcm_measurement_delta_position *p, aspn23_lcm_measurement_delta_position *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
