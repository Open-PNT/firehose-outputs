// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_type_direction_3d_to_point_h
#define _aspn23_lcm_type_direction_3d_to_point_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif

#include "aspn23_lcm_type_remote_point.h"
#include "aspn23_lcm_type_image_feature.h"
#include "aspn23_lcm_type_integrity.h"

/**
 * Azimuth (first component of obs) is the angle between the sensor frame x-axis (boresight) and
 * the projection of the point onto the sensor frame's x-y plane (positive right-hand rule about the
 * sensor frame z-axis). Elevation (second component of obs) is the angle between the sensor frame's
 * x-y plane and the vector from the sensor to the point (positive right-hand rule about the
 * intermediate axis formed by the azimuth rotation). The covariance is for the azimuth and elevation
 * errors. Units for the observation are in rad, covariance rad^2.
 */
#define ASPN23_LCM_TYPE_DIRECTION_3D_TO_POINT_REFERENCE_FRAME_AZ_EL 0

/**
 * The pixel coordinates of the point in an image (sub-pixel resolution is allowed). When combined
 * with the camera model, these can be converted to the NORMALIZED_IMAGE reference frame
 * representation. Units for the observation are in pixels, covariance pixels^2.
 */
#define ASPN23_LCM_TYPE_DIRECTION_3D_TO_POINT_REFERENCE_FRAME_PIXEL 1

/**
 * 2D position from the sensor frame origin to the point expressed in components y and z of the
 * sensor frame normalized by the boresight distance. The x component of the sensor frame is the
 * boresight axis. Units for the observation are m/m, covariance (m/m)^2.
 */
#define ASPN23_LCM_TYPE_DIRECTION_3D_TO_POINT_REFERENCE_FRAME_NORMALIZED_IMAGE 2

/**
 * 2D position from the sensor frame origin to the point expressed in components y and z of the
 * sensor frame normalized by the range to the point. The x component of the sensor frame is the
 * boresight axis. Units for the observation are m/m, covariance (m/m)^2.
 */
#define ASPN23_LCM_TYPE_DIRECTION_3D_TO_POINT_REFERENCE_FRAME_SINE_SPACE 3
/// No additional error model provided (num_error_model_params = 0).
#define ASPN23_LCM_TYPE_DIRECTION_3D_TO_POINT_ERROR_MODEL_NONE 0


/**
 * 3D direction to or from a point as defined by the reference enum. Information about the remote
 * point (its position and/or correspondence with previous or future appearances
 * of this same point) may be determined using remote_point.
 */
typedef struct _aspn23_lcm_type_direction_3d_to_point aspn23_lcm_type_direction_3d_to_point;
struct _aspn23_lcm_type_direction_3d_to_point
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_type_direction_3d_to_point;

    /**
     * Description: Position and/or correspondence information about the remote point.
     * Units: various
     */
    aspn23_lcm_type_remote_point remote_point;

    /**
     * Defines measurement reference frame.
     */
    int8_t     reference_frame;

    /**
     * Description: 3D direction to the remote point as defined in reference_frame.
     * Units: various
     * LCM Type: double[2]
     */
    double     obs[2];

    /**
     * Description: Covariance of the direction measurement as defined in reference_frame.
     * Units: various
     * LCM Type: double[2][2]
     */
    double     covariance[2][2];

    /**
     * Description: Switch for whether observation_characteristics is valid or not.
     * Units: none
     */
    int8_t     has_observation_characteristics;

    /**
     * Description: Image feature characteristics.
     * Units: various
     */
    aspn23_lcm_type_image_feature observation_characteristics;

    /**
     * Defines an optional error model for other than zero-mean, additive, white Gaussian noise (AWGN).
     */
    int8_t     error_model;

    /**
     * Description: Number of parameters required for the error model chosen.
     * Units: none
     */
    int32_t    num_error_model_params;

    /**
     * Description: Error model parameters that characterize the optional error model.
     * Units: various
     * LCM Type: double[num_error_model_params]
     */
    double     *error_model_params;

    /**
     * Description: Number of integrity values.
     * Units: none
     */
    int16_t    num_integrity;

    /**
     * Description: Measurement integrity. Includes the integrity method used and an integrity value
     * (which is to be interpreted based upon the integrity method). The intent of allowing num_integrity >
     * 1 is to report multiple integrity values based on multiple integrity methods.
     * Units: none
     * LCM Type: aspn23_lcm.type_integrity[num_integrity]
     */
    aspn23_lcm_type_integrity *integrity;
};

/**
 * Create a deep copy of a aspn23_lcm_type_direction_3d_to_point.
 * When no longer needed, destroy it with aspn23_lcm_type_direction_3d_to_point_destroy()
 */
aspn23_lcm_type_direction_3d_to_point* aspn23_lcm_type_direction_3d_to_point_copy(const aspn23_lcm_type_direction_3d_to_point* to_copy);

/**
 * Destroy an instance of aspn23_lcm_type_direction_3d_to_point created by aspn23_lcm_type_direction_3d_to_point_copy()
 */
void aspn23_lcm_type_direction_3d_to_point_destroy(aspn23_lcm_type_direction_3d_to_point* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_type_direction_3d_to_point_subscription_t aspn23_lcm_type_direction_3d_to_point_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_type_direction_3d_to_point is received.
 */
typedef void(*aspn23_lcm_type_direction_3d_to_point_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_type_direction_3d_to_point *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_type_direction_3d_to_point using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_type_direction_3d_to_point_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_type_direction_3d_to_point *msg);

/**
 * Subscribe to messages of type aspn23_lcm_type_direction_3d_to_point using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_type_direction_3d_to_point_subscription_t* aspn23_lcm_type_direction_3d_to_point_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_type_direction_3d_to_point_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_type_direction_3d_to_point_subscribe()
 */
int aspn23_lcm_type_direction_3d_to_point_unsubscribe(lcm_t *lcm, aspn23_lcm_type_direction_3d_to_point_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_type_direction_3d_to_point_subscription_set_queue_capacity(
    aspn23_lcm_type_direction_3d_to_point_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_type_direction_3d_to_point into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_type_direction_3d_to_point_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_type_direction_3d_to_point_encode(void *buf, int offset, int maxlen, const aspn23_lcm_type_direction_3d_to_point *p);

/**
 * Decode a message of type aspn23_lcm_type_direction_3d_to_point from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_type_direction_3d_to_point_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_type_direction_3d_to_point_decode(const void *buf, int offset, int maxlen, aspn23_lcm_type_direction_3d_to_point *msg);

/**
 * Release resources allocated by aspn23_lcm_type_direction_3d_to_point_decode()
 * @return 0
 */
int aspn23_lcm_type_direction_3d_to_point_decode_cleanup(aspn23_lcm_type_direction_3d_to_point *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_type_direction_3d_to_point
 */
int aspn23_lcm_type_direction_3d_to_point_encoded_size(const aspn23_lcm_type_direction_3d_to_point *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_type_direction_3d_to_point_get_hash(void);
uint64_t __aspn23_lcm_type_direction_3d_to_point_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_type_direction_3d_to_point_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_type_direction_3d_to_point *p, int elements);
int __aspn23_lcm_type_direction_3d_to_point_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_type_direction_3d_to_point *p, int elements);
int __aspn23_lcm_type_direction_3d_to_point_decode_array_cleanup(aspn23_lcm_type_direction_3d_to_point *p, int elements);
int __aspn23_lcm_type_direction_3d_to_point_encoded_array_size(const aspn23_lcm_type_direction_3d_to_point *p, int elements);
int __aspn23_lcm_type_direction_3d_to_point_clone_array(const aspn23_lcm_type_direction_3d_to_point *p, aspn23_lcm_type_direction_3d_to_point *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
