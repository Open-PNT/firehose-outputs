// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_metadata_IMU_h
#define _aspn23_lcm_metadata_IMU_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif

#include "aspn23_lcm_type_metadataheader.h"
#include "aspn23_lcm_type_timestamp.h"
#include "aspn23_lcm_type_mounting.h"

/**
 * All axes are identical and uncorrelated with each other axis (num_error_model_params = 14).
 * 1) accel_bias_sigma, units: m/s/s, description: Standard deviation of constant bias for
 * accelerometer measurements.
 * 2) accel_bias_mean, units: m/s/s, description: Mean of constant bias for accelerometer
 * measurements.
 * 3) accel_time_correlated_bias_sigma, units: m/s/s, description: Standard deviation of
 * zero-mean time-correlated bias for accelerometer measurement.
 * 4) accel_time_correlated_bias_time_constant, units: s, description: Time constant of
 * zero-mean time-correlated bias for accelerometer measurement.
 * 5) accel_scale_factor_mean, units: ppm, description: Mean of accelerometer scale factor
 * modeled as a constant with Gaussian uncertainty.
 * 6) accel_scale_factor_sigma, units: ppm, description: Standard deviation of accelerometer
 * scale factor modeled as a constant with Gaussian uncertainty.
 * 7) velocity_random_walk, units: m/s/sqrt(s), description: Velocity random walk due to
 * acceleration.
 * 8) gyro_bias_sigma, units: rad/s, description: Standard deviation of constant bias for gyro
 * measurements.
 * 9) gyro_bias_mean, units: rad/s, description: Mean of constant bias for gyro measurements.
 * 10) gyro_time_correlated_bias_sigma, units: rad/s, description: Standard deviation of
 * zero-mean time-correlated bias for gyro measurements.
 * 11) gyro_time_correlated_bias_time_constant, units: s, description: Time constant of
 * zero-mean time-correlated bias for gyro measurements.
 * 12) gyro_scale_factor_mean, units: ppm, description: Mean of gyro scale factor modeled as a
 * constant with Gaussian uncertainty.
 * 13) gyro_scale_factor_sigma, units: ppm, description: Standard deviation of gyro scale
 * factor modeled as a constant with Gaussian uncertainty.
 * 14) angular_random_walk, units: rad/sqrt(s), description: Angular random walk due to gyros.
 */
#define ASPN23_LCM_METADATA_IMU_ERROR_MODEL_BASIC 0

/// Metadata for inertial measurement unit.
typedef struct _aspn23_lcm_metadata_IMU aspn23_lcm_metadata_IMU;
struct _aspn23_lcm_metadata_IMU
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_metadata_IMU;

    /**
     * Description: Standard ASPN metadata header.
     * Units: none
     */
    aspn23_lcm_type_metadataheader info;

    /**
     * Description: Time at which the measurement is considered to be valid.
     * Units: none
     */
    aspn23_lcm_type_timestamp time_of_validity;

    /**
     * Description: Standard ASPN mounting information.
     * Units: none
     */
    aspn23_lcm_type_mounting mounting;

    /**
     * Allows the user to select an appropriate error model. Parameters are defined here. The number of
     * parameters and the values for those parameters are included in subsequent fields.
     */
    int8_t     error_model;

    /**
     * Description: Number of parameters required for the error model chosen.
     * Units: none
     */
    int32_t    num_error_model_params;

    /**
     * Description: Error model parameters that characterize the optional error model.
     * Units: various
     * LCM Type: double[num_error_model_params]
     */
    double     *error_model_params;
};

/**
 * Create a deep copy of a aspn23_lcm_metadata_IMU.
 * When no longer needed, destroy it with aspn23_lcm_metadata_IMU_destroy()
 */
aspn23_lcm_metadata_IMU* aspn23_lcm_metadata_IMU_copy(const aspn23_lcm_metadata_IMU* to_copy);

/**
 * Destroy an instance of aspn23_lcm_metadata_IMU created by aspn23_lcm_metadata_IMU_copy()
 */
void aspn23_lcm_metadata_IMU_destroy(aspn23_lcm_metadata_IMU* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_metadata_IMU_subscription_t aspn23_lcm_metadata_IMU_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_metadata_IMU is received.
 */
typedef void(*aspn23_lcm_metadata_IMU_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_metadata_IMU *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_metadata_IMU using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_metadata_IMU_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_metadata_IMU *msg);

/**
 * Subscribe to messages of type aspn23_lcm_metadata_IMU using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_metadata_IMU_subscription_t* aspn23_lcm_metadata_IMU_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_metadata_IMU_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_metadata_IMU_subscribe()
 */
int aspn23_lcm_metadata_IMU_unsubscribe(lcm_t *lcm, aspn23_lcm_metadata_IMU_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_metadata_IMU_subscription_set_queue_capacity(
    aspn23_lcm_metadata_IMU_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_metadata_IMU into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_metadata_IMU_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_metadata_IMU_encode(void *buf, int offset, int maxlen, const aspn23_lcm_metadata_IMU *p);

/**
 * Decode a message of type aspn23_lcm_metadata_IMU from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_metadata_IMU_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_metadata_IMU_decode(const void *buf, int offset, int maxlen, aspn23_lcm_metadata_IMU *msg);

/**
 * Release resources allocated by aspn23_lcm_metadata_IMU_decode()
 * @return 0
 */
int aspn23_lcm_metadata_IMU_decode_cleanup(aspn23_lcm_metadata_IMU *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_metadata_IMU
 */
int aspn23_lcm_metadata_IMU_encoded_size(const aspn23_lcm_metadata_IMU *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_metadata_IMU_get_hash(void);
uint64_t __aspn23_lcm_metadata_IMU_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_metadata_IMU_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_metadata_IMU *p, int elements);
int __aspn23_lcm_metadata_IMU_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_metadata_IMU *p, int elements);
int __aspn23_lcm_metadata_IMU_decode_array_cleanup(aspn23_lcm_metadata_IMU *p, int elements);
int __aspn23_lcm_metadata_IMU_encoded_array_size(const aspn23_lcm_metadata_IMU *p, int elements);
int __aspn23_lcm_metadata_IMU_clone_array(const aspn23_lcm_metadata_IMU *p, aspn23_lcm_metadata_IMU *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
