// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_type_metadataheader_h
#define _aspn23_lcm_type_metadataheader_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif

#include "aspn23_lcm_type_header.h"
/// Sensor metadata header.
typedef struct _aspn23_lcm_type_metadataheader aspn23_lcm_type_metadataheader;
struct _aspn23_lcm_type_metadataheader
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_type_metadataheader;

    /**
     * Description: Standard ASPN measurement header.
     * Units: none
     */
    aspn23_lcm_type_header header;

    /**
     * Description: Describes the device used to generate the measurement (user defined).
     * Units: none
     * LCM Type: string
     */
    char*      sensor_description;

    /**
     * Description: Optional nominal time interval between each measurement. Actual time interval may
     * vary. Do not report a delta_t_nom if the nominal time interval is not known ahead of time.
     * Units: seconds
     */
    double     delta_t_nom;

    /**
     * Description: Identifier for the timestamp's clock/timing source. See clock_identifiers.md for a
     * full description, but the summary is as follows:
     * 0 = ASPN System Time
     * 1 = International Atomic Time (TAI)
     * 2 = Universal Coordinated Time (UTC)
     * 3 = GPS System Time
     * 4 = Galileo System Time
     * 5 = GLONASS System Time
     * 6 = BeiDou System Time
     * 7-50: Reserved for future additional time scale representations.
     *
     * Each message source must provide metadata information (the method used to provide the metadata
     * information depends on the system, e.g., over the wire, written to file, etc.), and the information
     * contained in this type (type_metadataheader) will be included in that information. In this manner,
     * each message source's clock ID will be defined. If the clock source is updated, new metadata must be
     * provided with an updated clock ID.
     * If used, ASPN system time shall be represented as a monotonically increasing quantity defined by
     * the system. System time zero epoch must be defined. For example, system time may be the time
     * difference with respect to a system power on event. Other user-defined clocks/timing sources must be
     * similarly defined by the system designer.
     * Units: none
     */
    int16_t    timestamp_clock_id;

    /**
     * Description: Defines how many decimal digits of precision are represented in the timestamp after
     * the decimal point. The precision of the timestamps is 10E-N, where N is digits_of_precision. A value
     * of 3 would mean that the timestamp has precision at the 10E-3 (millisecond) level, a value of 6
     * would mean the timestamp has precision at the 10E-6 (microsecond) level, etc.
     * If an implementation is using 64bit integers for their internal time representations, digits of
     * precision may be ignored with no additional complexity. If an implementation is converting to a
     * double, because of their choice of internal representation (which likely most estimators would use),
     * then knowing digits of precision would be important to avoid losing resolution.
     * Units: none
     */
    int16_t    digits_of_precision;
};

/**
 * Create a deep copy of a aspn23_lcm_type_metadataheader.
 * When no longer needed, destroy it with aspn23_lcm_type_metadataheader_destroy()
 */
aspn23_lcm_type_metadataheader* aspn23_lcm_type_metadataheader_copy(const aspn23_lcm_type_metadataheader* to_copy);

/**
 * Destroy an instance of aspn23_lcm_type_metadataheader created by aspn23_lcm_type_metadataheader_copy()
 */
void aspn23_lcm_type_metadataheader_destroy(aspn23_lcm_type_metadataheader* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_type_metadataheader_subscription_t aspn23_lcm_type_metadataheader_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_type_metadataheader is received.
 */
typedef void(*aspn23_lcm_type_metadataheader_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_type_metadataheader *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_type_metadataheader using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_type_metadataheader_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_type_metadataheader *msg);

/**
 * Subscribe to messages of type aspn23_lcm_type_metadataheader using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_type_metadataheader_subscription_t* aspn23_lcm_type_metadataheader_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_type_metadataheader_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_type_metadataheader_subscribe()
 */
int aspn23_lcm_type_metadataheader_unsubscribe(lcm_t *lcm, aspn23_lcm_type_metadataheader_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_type_metadataheader_subscription_set_queue_capacity(
    aspn23_lcm_type_metadataheader_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_type_metadataheader into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_type_metadataheader_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_type_metadataheader_encode(void *buf, int offset, int maxlen, const aspn23_lcm_type_metadataheader *p);

/**
 * Decode a message of type aspn23_lcm_type_metadataheader from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_type_metadataheader_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_type_metadataheader_decode(const void *buf, int offset, int maxlen, aspn23_lcm_type_metadataheader *msg);

/**
 * Release resources allocated by aspn23_lcm_type_metadataheader_decode()
 * @return 0
 */
int aspn23_lcm_type_metadataheader_decode_cleanup(aspn23_lcm_type_metadataheader *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_type_metadataheader
 */
int aspn23_lcm_type_metadataheader_encoded_size(const aspn23_lcm_type_metadataheader *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_type_metadataheader_get_hash(void);
uint64_t __aspn23_lcm_type_metadataheader_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_type_metadataheader_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_type_metadataheader *p, int elements);
int __aspn23_lcm_type_metadataheader_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_type_metadataheader *p, int elements);
int __aspn23_lcm_type_metadataheader_decode_array_cleanup(aspn23_lcm_type_metadataheader *p, int elements);
int __aspn23_lcm_type_metadataheader_encoded_array_size(const aspn23_lcm_type_metadataheader *p, int elements);
int __aspn23_lcm_type_metadataheader_clone_array(const aspn23_lcm_type_metadataheader *p, aspn23_lcm_type_metadataheader *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
