// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_type_satnav_sv_data_h
#define _aspn23_lcm_type_satnav_sv_data_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif

#include "aspn23_lcm_type_satnav_satellite_system.h"
#include "aspn23_lcm_type_satnav_time.h"

/**
 * To be used when a system only uses one ephemeris representation, so knowing the satellite_system
 * fully defines what ephemeris is being used.
 */
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_EPHEMERIS_TYPE_SET_BY_SYSTEM 0
/// Calculated using legacy GPS (LNAV) messages
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_EPHEMERIS_TYPE_GPS_LNAV 1
/// Calculated using GPS CNAV messages
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_EPHEMERIS_TYPE_GPS_CNAV 2
/// Calculated using GPS MNAV messages
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_EPHEMERIS_TYPE_GPS_MNAV 3
/// International Terrestrial Reference Frame
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_COORDINATE_FRAME_ITRF 0
/// Earth-centered earth-fixed frame as defined by WGS-84 (nearly identical with ITRF)
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_COORDINATE_FRAME_ECEF 1
/// Galileo Terrestrial Reference Frame (nearly identical with ITRF)
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_COORDINATE_FRAME_GTRF 2
/// Parametri Zemli 1990 (PZ-90) reference frame
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_COORDINATE_FRAME_PZ90 3

/**
 * group_delay_vector[0] is legacy Tgd as defined in ICD-GPS-200L Section 20.3.3.3.3.2. All other
 * terms not used.
 */
#define ASPN23_LCM_TYPE_SATNAV_SV_DATA_GROUP_DELAY_ENUM_TGD_LNAV 0

/// Satellite position, velocity and clock error at a particular time epoch
typedef struct _aspn23_lcm_type_satnav_sv_data aspn23_lcm_type_satnav_sv_data;
struct _aspn23_lcm_type_satnav_sv_data
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_type_satnav_sv_data;

    /**
     * Description: PRN code which identifies satellite (or slot number, in the case of GLONASS) of
     * this ephemeris.
     * Units: none
     */
    int16_t    prn;

    /**
     * Description: Describes the Satellite System which was used to generate the satellite information
     * provided in this message.
     * Units: none
     */
    aspn23_lcm_type_satnav_satellite_system satellite_system;

    /**
     * Provides further clarification of which ephemeris was used to generate the satellite information
     * provided in this message for systems which have more than one ephemeris representation.
     */
    int8_t     ephemeris_type;

    /**
     * Description: Receiver time at which the data provided in this message is valid.
     * Units: none
     */
    aspn23_lcm_type_satnav_time sv_data_time;

    /**
     * Coordinate frame system used to describe satellite position and velocity.
     */
    int8_t     coordinate_frame;

    /**
     * Description: Satellite position in frame specified by coordinate_frame at time specified by
     * sv_data_time_week_number and sv_data_time_seconds_of_week.
     * Units: m
     * LCM Type: double[3]
     */
    double     sv_pos[3];

    /**
     * Description: Satellite velocity in frame specified by coordinate_frame at time specified by
     * sv_data_time_week_number and sv_data_time_seconds_of_week.
     * Units: m/s
     * LCM Type: double[3]
     */
    double     sv_vel[3];

    /**
     * Description: Satellite clock bias at the sv_data_time, used to correct the satellite time like
     * delta_t_sv in equation (1) of ICD-GPS-200L, Section 20.3.3.3.3.1.
     * Units: sec
     */
    double     sv_clock_bias;

    /**
     * Description: Satellite clock drift rate at the sv_data_time. By way of example, for GPS this
     * would be af1 in equation (2) of ICD-GPS-200L, Section 20.3.3.3.3.1.
     * Units: sec/sec
     */
    double     sv_clock_drift;

    /**
     * Describes how to interpret group delay terms, because they vary by ephemeris type. Descriptions
     * assume zero-indexing.
     */
    int8_t     group_delay_enum;

    /**
     * Description: Group delay terms, with interpretation provided by group_delay_enum. (Need to
     * verify that four is sufficient for all ephemeris.)
     * Units: sec
     * LCM Type: float[4]
     */
    float      group_delay_vector[4];
};

/**
 * Create a deep copy of a aspn23_lcm_type_satnav_sv_data.
 * When no longer needed, destroy it with aspn23_lcm_type_satnav_sv_data_destroy()
 */
aspn23_lcm_type_satnav_sv_data* aspn23_lcm_type_satnav_sv_data_copy(const aspn23_lcm_type_satnav_sv_data* to_copy);

/**
 * Destroy an instance of aspn23_lcm_type_satnav_sv_data created by aspn23_lcm_type_satnav_sv_data_copy()
 */
void aspn23_lcm_type_satnav_sv_data_destroy(aspn23_lcm_type_satnav_sv_data* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_type_satnav_sv_data_subscription_t aspn23_lcm_type_satnav_sv_data_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_type_satnav_sv_data is received.
 */
typedef void(*aspn23_lcm_type_satnav_sv_data_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_type_satnav_sv_data *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_type_satnav_sv_data using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_type_satnav_sv_data_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_type_satnav_sv_data *msg);

/**
 * Subscribe to messages of type aspn23_lcm_type_satnav_sv_data using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_type_satnav_sv_data_subscription_t* aspn23_lcm_type_satnav_sv_data_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_type_satnav_sv_data_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_type_satnav_sv_data_subscribe()
 */
int aspn23_lcm_type_satnav_sv_data_unsubscribe(lcm_t *lcm, aspn23_lcm_type_satnav_sv_data_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_type_satnav_sv_data_subscription_set_queue_capacity(
    aspn23_lcm_type_satnav_sv_data_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_type_satnav_sv_data into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_type_satnav_sv_data_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_type_satnav_sv_data_encode(void *buf, int offset, int maxlen, const aspn23_lcm_type_satnav_sv_data *p);

/**
 * Decode a message of type aspn23_lcm_type_satnav_sv_data from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_type_satnav_sv_data_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_type_satnav_sv_data_decode(const void *buf, int offset, int maxlen, aspn23_lcm_type_satnav_sv_data *msg);

/**
 * Release resources allocated by aspn23_lcm_type_satnav_sv_data_decode()
 * @return 0
 */
int aspn23_lcm_type_satnav_sv_data_decode_cleanup(aspn23_lcm_type_satnav_sv_data *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_type_satnav_sv_data
 */
int aspn23_lcm_type_satnav_sv_data_encoded_size(const aspn23_lcm_type_satnav_sv_data *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_type_satnav_sv_data_get_hash(void);
uint64_t __aspn23_lcm_type_satnav_sv_data_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_type_satnav_sv_data_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_type_satnav_sv_data *p, int elements);
int __aspn23_lcm_type_satnav_sv_data_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_type_satnav_sv_data *p, int elements);
int __aspn23_lcm_type_satnav_sv_data_decode_array_cleanup(aspn23_lcm_type_satnav_sv_data *p, int elements);
int __aspn23_lcm_type_satnav_sv_data_encoded_array_size(const aspn23_lcm_type_satnav_sv_data *p, int elements);
int __aspn23_lcm_type_satnav_sv_data_clone_array(const aspn23_lcm_type_satnav_sv_data *p, aspn23_lcm_type_satnav_sv_data *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
