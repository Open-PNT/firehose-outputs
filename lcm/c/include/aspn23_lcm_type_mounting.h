// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_type_mounting_h
#define _aspn23_lcm_type_mounting_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif


/**
 * Describes the translational and angular offset between the sensor frame and the platform body frame.
 * Platform body frame is defined using axes in forward, right, and down convention, and the origin is
 * system defined. This type also contains the uncertainty associated with the translational and
 * angular offset.
 */
typedef struct _aspn23_lcm_type_mounting aspn23_lcm_type_mounting;
struct _aspn23_lcm_type_mounting
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_type_mounting;

    /**
     * Description: 3x1 lever arm vector describing the sensor position in the platform body frame.
     * Units: m
     * LCM Type: double[3]
     */
    double     lever_arm[3];

    /**
     * Description: 3x1 lever arm uncertainty vector as standard deviations in the platform body frame.
     * Units: m
     * LCM Type: double[3]
     */
    double     lever_arm_sigma[3];

    /**
     * Description: Four element quaternion, q = [a, b, c, d], where a = cos(phi/2), b =
     * (phi_x/phi)*sin(phi/2), c = (phi_y/phi)*sin(phi/2), and d = (phi_z/phi)*sin(phi/2). In this
     * description, the vector [phi_x, phi_y, phi_z] represents the rotation vector that describes the
     * frame rotation to be applied to the "reference" frame (ECI, ECEF, or NED) to rotate it into the axes
     * that describe the measured attitude, and the value phi is the magnitude of the [phi_x, phi_y, phi_z]
     * vector.
     * Orientation is optional in the sense that orientation shall be specified except in the case that
     * orientation is meaningless, for example, in the case of an RF antenna mounting.
     * See "conventions" documentation for more detailed information.
     * Units: none
     * LCM Type: double[4]
     */
    double     orientation_quaternion[4];

    /**
     * Description: Tilt error covariance matrix. This matrix represents the uncertainty in the "tilt
     * errors" that represent the additional rotation to be applied to the provided attitude quaternion in
     * order to convert it to the true attitude with no errors. By convention, these "tilt errors" are
     * expressed in the reference frame (ECI, ECEF, or NED).
     * Tilt error covariance is optional only because orientation itself is optional. Orientation error
     * covariance shall be provided if orientation is provided.
     * See "conventions" documentation for more detailed information.
     * Units: rad^2
     * LCM Type: double[3][3]
     */
    double     orientation_tilt_error_covariance[3][3];
};

/**
 * Create a deep copy of a aspn23_lcm_type_mounting.
 * When no longer needed, destroy it with aspn23_lcm_type_mounting_destroy()
 */
aspn23_lcm_type_mounting* aspn23_lcm_type_mounting_copy(const aspn23_lcm_type_mounting* to_copy);

/**
 * Destroy an instance of aspn23_lcm_type_mounting created by aspn23_lcm_type_mounting_copy()
 */
void aspn23_lcm_type_mounting_destroy(aspn23_lcm_type_mounting* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_type_mounting_subscription_t aspn23_lcm_type_mounting_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_type_mounting is received.
 */
typedef void(*aspn23_lcm_type_mounting_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_type_mounting *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_type_mounting using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_type_mounting_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_type_mounting *msg);

/**
 * Subscribe to messages of type aspn23_lcm_type_mounting using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_type_mounting_subscription_t* aspn23_lcm_type_mounting_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_type_mounting_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_type_mounting_subscribe()
 */
int aspn23_lcm_type_mounting_unsubscribe(lcm_t *lcm, aspn23_lcm_type_mounting_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_type_mounting_subscription_set_queue_capacity(
    aspn23_lcm_type_mounting_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_type_mounting into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_type_mounting_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_type_mounting_encode(void *buf, int offset, int maxlen, const aspn23_lcm_type_mounting *p);

/**
 * Decode a message of type aspn23_lcm_type_mounting from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_type_mounting_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_type_mounting_decode(const void *buf, int offset, int maxlen, aspn23_lcm_type_mounting *msg);

/**
 * Release resources allocated by aspn23_lcm_type_mounting_decode()
 * @return 0
 */
int aspn23_lcm_type_mounting_decode_cleanup(aspn23_lcm_type_mounting *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_type_mounting
 */
int aspn23_lcm_type_mounting_encoded_size(const aspn23_lcm_type_mounting *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_type_mounting_get_hash(void);
uint64_t __aspn23_lcm_type_mounting_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_type_mounting_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_type_mounting *p, int elements);
int __aspn23_lcm_type_mounting_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_type_mounting *p, int elements);
int __aspn23_lcm_type_mounting_decode_array_cleanup(aspn23_lcm_type_mounting *p, int elements);
int __aspn23_lcm_type_mounting_encoded_array_size(const aspn23_lcm_type_mounting *p, int elements);
int __aspn23_lcm_type_mounting_clone_array(const aspn23_lcm_type_mounting *p, aspn23_lcm_type_mounting *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
