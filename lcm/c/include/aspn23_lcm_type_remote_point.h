// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#ifndef _aspn23_lcm_type_remote_point_h
#define _aspn23_lcm_type_remote_point_h

#include <stdint.h>
#include <stdlib.h>
#include <lcm/lcm_coretypes.h>
#include <lcm/lcm.h>

#ifdef __cplusplus
extern "C" {
#endif


/**
 * No reference frame. This is only valid if position is not included
 * (included_terms is 0 or 1).
 */
#define ASPN23_LCM_TYPE_REMOTE_POINT_POSITION_REFERENCE_FRAME_NONE 0

/**
 * Point position relative to the WGS-84 reference expressed using position1 as geodetic latitude
 * in radians, position2 as longitude in radians, and position3 as geodetic altitude (or height
 * above the WGS-84 ellipsoid) in meters. Each term is optional.
 * position_covariance is given using north-south position error in meters (an expression of
 * geodetic latitude error), east-west position error in meters (an expression of longitude
 * error), and geodetic altitude error in meters. In the case of nulled position components, the
 * corresponding error covariance terms are omitted.
 */
#define ASPN23_LCM_TYPE_REMOTE_POINT_POSITION_REFERENCE_FRAME_GEODETIC 1


/**
 * ASPN custom type representing a point. This includes optional information
 * (indicated by included_terms) for the position and a correspondence ID for the
 * point.
 */
typedef struct _aspn23_lcm_type_remote_point aspn23_lcm_type_remote_point;
struct _aspn23_lcm_type_remote_point
{

    /**
     * Non ASPN. Do not use. Extra field encoding the struct name to disambiguate LCM type fingerprint hashes.
     */
    int8_t     icd_type_remote_point;

    /**
     * Description: Indicates which information is included about the point
     * 0 = no information included (unknown point)
     * 1 = id included (corresponded point)
     * 2 = position included (known point)
     * 3 = id and position included
     * Units: none
     */
    int16_t    included_terms;

    /**
     * Description: Unique identification number assigned by the sensor. Points that have the same id
     * from a sensor are multiple instances of the same point as determined by a sensor's point
     * correspondence.
     * Units: none
     */
    int64_t    id;

    /**
     * Defines position reference.
     */
    int8_t     position_reference_frame;

    /**
     * Description: First position term as defined in position_reference_frame.
     * Units: various
     */
    double     position1;

    /**
     * Description: Second position term as defined in position_reference_frame.
     * Units: various
     */
    double     position2;

    /**
     * Description: Third position term as defined in position_reference_frame.
     * Units: various
     */
    double     position3;

    /**
     * Description: Dimension of position provided.
     * Units: none
     */
    int16_t    num_position_components;

    /**
     * Description: Position error covariance (or variance depending on num_position_components) as
     * defined in position_reference_frame.
     * Units: m^2
     * LCM Type: double[num_position_components][num_position_components]
     */
    double     **position_covariance;
};

/**
 * Create a deep copy of a aspn23_lcm_type_remote_point.
 * When no longer needed, destroy it with aspn23_lcm_type_remote_point_destroy()
 */
aspn23_lcm_type_remote_point* aspn23_lcm_type_remote_point_copy(const aspn23_lcm_type_remote_point* to_copy);

/**
 * Destroy an instance of aspn23_lcm_type_remote_point created by aspn23_lcm_type_remote_point_copy()
 */
void aspn23_lcm_type_remote_point_destroy(aspn23_lcm_type_remote_point* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _aspn23_lcm_type_remote_point_subscription_t aspn23_lcm_type_remote_point_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * aspn23_lcm_type_remote_point is received.
 */
typedef void(*aspn23_lcm_type_remote_point_handler_t)(
    const lcm_recv_buf_t *rbuf, const char *channel,
    const aspn23_lcm_type_remote_point *msg, void *userdata);

/**
 * Publish a message of type aspn23_lcm_type_remote_point using LCM.
 *
 * @param lcm The LCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means LCM has transferred
 * responsibility of the message data to the OS.
 */
int aspn23_lcm_type_remote_point_publish(lcm_t *lcm, const char *channel, const aspn23_lcm_type_remote_point *msg);

/**
 * Subscribe to messages of type aspn23_lcm_type_remote_point using LCM.
 *
 * @param lcm The LCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by LCM when a message is
 *     received. This function is invoked by LCM during calls to lcm_handle()
 *     and lcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return 0 on success, <0 if an error occured
 */
aspn23_lcm_type_remote_point_subscription_t* aspn23_lcm_type_remote_point_subscribe(
    lcm_t *lcm, const char *channel, aspn23_lcm_type_remote_point_handler_t handler, void *userdata);

/**
 * Removes and destroys a subscription created by aspn23_lcm_type_remote_point_subscribe()
 */
int aspn23_lcm_type_remote_point_unsubscribe(lcm_t *lcm, aspn23_lcm_type_remote_point_subscription_t* hid);

/**
 * Sets the queue capacity for a subscription.
 * Some LCM providers (e.g., the default multicast provider) are implemented
 * using a background receive thread that constantly revceives messages from
 * the network.  As these messages are received, they are buffered on
 * per-subscription queues until dispatched by lcm_handle().  This function
 * how many messages are queued before dropping messages.
 *
 * @param subs the subscription to modify.
 * @param num_messages The maximum number of messages to queue
 *  on the subscription.
 * @return 0 on success, <0 if an error occured
 */
int aspn23_lcm_type_remote_point_subscription_set_queue_capacity(
    aspn23_lcm_type_remote_point_subscription_t* subs, int num_messages);

/**
 * Encode a message of type aspn23_lcm_type_remote_point into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to aspn23_lcm_type_remote_point_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int aspn23_lcm_type_remote_point_encode(void *buf, int offset, int maxlen, const aspn23_lcm_type_remote_point *p);

/**
 * Decode a message of type aspn23_lcm_type_remote_point from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with aspn23_lcm_type_remote_point_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int aspn23_lcm_type_remote_point_decode(const void *buf, int offset, int maxlen, aspn23_lcm_type_remote_point *msg);

/**
 * Release resources allocated by aspn23_lcm_type_remote_point_decode()
 * @return 0
 */
int aspn23_lcm_type_remote_point_decode_cleanup(aspn23_lcm_type_remote_point *p);

/**
 * Check how many bytes are required to encode a message of type aspn23_lcm_type_remote_point
 */
int aspn23_lcm_type_remote_point_encoded_size(const aspn23_lcm_type_remote_point *p);

// LCM support functions. Users should not call these
int64_t __aspn23_lcm_type_remote_point_get_hash(void);
uint64_t __aspn23_lcm_type_remote_point_hash_recursive(const __lcm_hash_ptr *p);
int __aspn23_lcm_type_remote_point_encode_array(
    void *buf, int offset, int maxlen, const aspn23_lcm_type_remote_point *p, int elements);
int __aspn23_lcm_type_remote_point_decode_array(
    const void *buf, int offset, int maxlen, aspn23_lcm_type_remote_point *p, int elements);
int __aspn23_lcm_type_remote_point_decode_array_cleanup(aspn23_lcm_type_remote_point *p, int elements);
int __aspn23_lcm_type_remote_point_encoded_array_size(const aspn23_lcm_type_remote_point *p, int elements);
int __aspn23_lcm_type_remote_point_clone_array(const aspn23_lcm_type_remote_point *p, aspn23_lcm_type_remote_point *q, int elements);

#ifdef __cplusplus
}
#endif

#endif
