// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen 1.5.2


/**
 * This code is generated via firehose.
 * DO NOT hand edit code.  Make any changes required using the firehose repo instead
 */
#include <string.h>
#include "aspn23_lcm_type_metadataheader.h"

static int __aspn23_lcm_type_metadataheader_hash_computed;
static uint64_t __aspn23_lcm_type_metadataheader_hash;

uint64_t __aspn23_lcm_type_metadataheader_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __aspn23_lcm_type_metadataheader_get_hash)
            return 0;

    __lcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = __aspn23_lcm_type_metadataheader_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0xe9550c5c6e6fe245LL
         + __int8_t_hash_recursive(&cp)
         + __aspn23_lcm_type_header_hash_recursive(&cp)
         + __string_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __aspn23_lcm_type_metadataheader_get_hash(void)
{
    if (!__aspn23_lcm_type_metadataheader_hash_computed) {
        __aspn23_lcm_type_metadataheader_hash = (int64_t)__aspn23_lcm_type_metadataheader_hash_recursive(NULL);
        __aspn23_lcm_type_metadataheader_hash_computed = 1;
    }

    return __aspn23_lcm_type_metadataheader_hash;
}

int __aspn23_lcm_type_metadataheader_encode_array(void *buf, int offset, int maxlen, const aspn23_lcm_type_metadataheader *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        thislen = __int8_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].icd_type_metadataheader), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __aspn23_lcm_type_header_encode_array(buf, offset + pos, maxlen - pos, &(p[element].header), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __string_encode_array(buf, offset + pos, maxlen - pos, &(p[element].sensor_description), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].delta_t_nom), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].timestamp_clock_id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].digits_of_precision), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int aspn23_lcm_type_metadataheader_encode(void *buf, int offset, int maxlen, const aspn23_lcm_type_metadataheader *p)
{
    int pos = 0, thislen;
    int64_t hash = __aspn23_lcm_type_metadataheader_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __aspn23_lcm_type_metadataheader_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __aspn23_lcm_type_metadataheader_encoded_array_size(const aspn23_lcm_type_metadataheader *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __int8_t_encoded_array_size(&(p[element].icd_type_metadataheader), 1);

        size += __aspn23_lcm_type_header_encoded_array_size(&(p[element].header), 1);

        size += __string_encoded_array_size(&(p[element].sensor_description), 1);

        size += __double_encoded_array_size(&(p[element].delta_t_nom), 1);

        size += __int16_t_encoded_array_size(&(p[element].timestamp_clock_id), 1);

        size += __int16_t_encoded_array_size(&(p[element].digits_of_precision), 1);

    }
    return size;
}

int aspn23_lcm_type_metadataheader_encoded_size(const aspn23_lcm_type_metadataheader *p)
{
    return 8 + __aspn23_lcm_type_metadataheader_encoded_array_size(p, 1);
}

int __aspn23_lcm_type_metadataheader_decode_array(const void *buf, int offset, int maxlen, aspn23_lcm_type_metadataheader *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int8_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].icd_type_metadataheader), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __aspn23_lcm_type_header_decode_array(buf, offset + pos, maxlen - pos, &(p[element].header), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __string_decode_array(buf, offset + pos, maxlen - pos, &(p[element].sensor_description), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].delta_t_nom), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].timestamp_clock_id), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].digits_of_precision), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __aspn23_lcm_type_metadataheader_decode_array_cleanup(aspn23_lcm_type_metadataheader *p, int elements)
{
    (void)p;
    int element;
    for (element = 0; element < elements; element++) {

        __int8_t_decode_array_cleanup(&(p[element].icd_type_metadataheader), 1);

        __aspn23_lcm_type_header_decode_array_cleanup(&(p[element].header), 1);

        __string_decode_array_cleanup(&(p[element].sensor_description), 1);

        __double_decode_array_cleanup(&(p[element].delta_t_nom), 1);

        __int16_t_decode_array_cleanup(&(p[element].timestamp_clock_id), 1);

        __int16_t_decode_array_cleanup(&(p[element].digits_of_precision), 1);

    }
    return 0;
}

int aspn23_lcm_type_metadataheader_decode(const void *buf, int offset, int maxlen, aspn23_lcm_type_metadataheader *p)
{
    int pos = 0, thislen;
    int64_t hash = __aspn23_lcm_type_metadataheader_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __aspn23_lcm_type_metadataheader_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int aspn23_lcm_type_metadataheader_decode_cleanup(aspn23_lcm_type_metadataheader *p)
{
    return __aspn23_lcm_type_metadataheader_decode_array_cleanup(p, 1);
}

int __aspn23_lcm_type_metadataheader_clone_array(const aspn23_lcm_type_metadataheader *p, aspn23_lcm_type_metadataheader *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int8_t_clone_array(&(p[element].icd_type_metadataheader), &(q[element].icd_type_metadataheader), 1);

        __aspn23_lcm_type_header_clone_array(&(p[element].header), &(q[element].header), 1);

        __string_clone_array(&(p[element].sensor_description), &(q[element].sensor_description), 1);

        __double_clone_array(&(p[element].delta_t_nom), &(q[element].delta_t_nom), 1);

        __int16_t_clone_array(&(p[element].timestamp_clock_id), &(q[element].timestamp_clock_id), 1);

        __int16_t_clone_array(&(p[element].digits_of_precision), &(q[element].digits_of_precision), 1);

    }
    return 0;
}

aspn23_lcm_type_metadataheader *aspn23_lcm_type_metadataheader_copy(const aspn23_lcm_type_metadataheader *p)
{
    aspn23_lcm_type_metadataheader *q = (aspn23_lcm_type_metadataheader*) malloc(sizeof(aspn23_lcm_type_metadataheader));
    __aspn23_lcm_type_metadataheader_clone_array(p, q, 1);
    return q;
}

void aspn23_lcm_type_metadataheader_destroy(aspn23_lcm_type_metadataheader *p)
{
    __aspn23_lcm_type_metadataheader_decode_array_cleanup(p, 1);
    free(p);
}

int aspn23_lcm_type_metadataheader_publish(lcm_t *lc, const char *channel, const aspn23_lcm_type_metadataheader *p)
{
      int max_data_size = aspn23_lcm_type_metadataheader_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = aspn23_lcm_type_metadataheader_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _aspn23_lcm_type_metadataheader_subscription_t {
    aspn23_lcm_type_metadataheader_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void aspn23_lcm_type_metadataheader_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    aspn23_lcm_type_metadataheader p;
    memset(&p, 0, sizeof(aspn23_lcm_type_metadataheader));
    status = aspn23_lcm_type_metadataheader_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding aspn23_lcm_type_metadataheader!!!\n", status);
        return;
    }

    aspn23_lcm_type_metadataheader_subscription_t *h = (aspn23_lcm_type_metadataheader_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    aspn23_lcm_type_metadataheader_decode_cleanup (&p);
}

aspn23_lcm_type_metadataheader_subscription_t* aspn23_lcm_type_metadataheader_subscribe (lcm_t *lcm,
                    const char *channel,
                    aspn23_lcm_type_metadataheader_handler_t f, void *userdata)
{
    aspn23_lcm_type_metadataheader_subscription_t *n = (aspn23_lcm_type_metadataheader_subscription_t*)
                       malloc(sizeof(aspn23_lcm_type_metadataheader_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 aspn23_lcm_type_metadataheader_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg aspn23_lcm_type_metadataheader LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int aspn23_lcm_type_metadataheader_subscription_set_queue_capacity (aspn23_lcm_type_metadataheader_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int aspn23_lcm_type_metadataheader_unsubscribe(lcm_t *lcm, aspn23_lcm_type_metadataheader_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe aspn23_lcm_type_metadataheader_handler %p!\n", (void*)hid);
        return -1;
    }
    free (hid);
    return 0;
}

